# SelahCue — Build State

Lightweight pointer only. ClickUp is the delivery source of truth. Do not duplicate the backlog here.

- Product: **SelahCue** (cross-platform church presentation & ministry-assistance app)
- Build Control task: https://app.clickup.com/t/86ajnx548 (`86ajnx548`)
- ClickUp delivery list: `SelahCue — Delivery` (`901327960792`) in folder `SelahCue` (`901318653689`), space `First Pavilion (Engineering)` (`90136583508`)
- Build Goal Contract: `docs/delivery/goals/BUILD-selahcue.md`
- Current stage: **Stage 7 — Implementation foundation** (batches 7a–7k done, at gate)
- Stage state: `GATE_REVIEW` (awaiting user gate decision — this gate authorises production implementation)
- Last approved gate: Gate 6 (Stage 6) — user replied `continue`, authorising Stage 7 (production implementation)
- Execution engine: `goal`
- Execution model: **Opus 4.8 (1M context)** — Stage 2+ runs under the current session model.

## Stage 2 outcome (2026-07-23)

Discovery complete. 6 parallel research specialists + a 4-lens independent review workflow (PASS WITH CONDITIONS, 0 blockers) + a follow-up adjacent-product study (condition C1). Integrity conditions resolved; architecture conditions formally deferred with recorded recommendations. Artefacts in docs/research/, docs/business/, docs/security/reviews/. Awaiting Stage 2 gate decision.

Key Stage 2 artefacts: DISCOVERY-REPORT.md, EVIDENCE-REGISTER.md, OPEN-DECISIONS.md, DISCOVERY-REVIEW.md, CONDITION-DISPOSITIONS.md (all under docs/research/); Stage 2 contract docs/delivery/goals/STAGE2-discovery.md.

**Refine (2026-07-23):** user de-scoped **TTS** entirely — removed from roadmap ([DEC-001](../decisions/DECISION-LOG.md)). Discovery artefacts, OPEN-DECISIONS OD-02, risk register RISK-008, and Build Goal Contract non-goals updated.

## Stage 3 outcome (2026-07-23)

PRD authored: `docs/product/prds/SelahCue-PRD.md` — 168 FR + 26 NFR + 10 FLOW + 13 RISK + 10 METRIC, all sections, testable acceptance criteria, MVP/R2-R6/non-goal boundary, full traceability. PM artifact validator `scripts/validate_prd.py` built (closes RISK-006) and passes. Independent 4-lens pre-audit review (`docs/product/audits/PRD-REVIEW-stage3.md`): PASS WITH CONDITIONS, 0 blockers; all 10 majors + 12 minors discharged (PRD §34).

## Stage 4 outcome (2026-07-23)

Formal 5-auditor independent PRD audit (`docs/product/audits/PRD-AUDIT-stage4.md`): PASS WITH CONDITIONS, **0 blockers**, 17 majors, 16 minors. All 17 majors discharged (PRD §35, adding FR-169…FR-177, NFR-026/027, RISK-014, AS-6) + material minors. Fresh-context re-audit (`docs/product/audits/PRD-AUDIT-stage4-reaudit.md`): **verdict PASS**, 17/17 discharged, 0 regressions; 5 advisory doc-hygiene minors also folded in. PRD now 177 FR + 27 NFR; validator passes. **PRD has cleared its mandatory independent audit.** Stakeholder report added: `docs/stakeholder/SelahCue-Build-Report.pdf`.

## Stage 5 outcome (2026-07-23)

Architecture spine (`docs/architecture/ARCHITECTURE.md`) + **16 ADRs** (`docs/architecture/adr/`) + UX (`docs/design/`: UX-FLOWS 12 flows, UX-STATE-MATRIX, COMPONENT-SPECS, UX-CANONICAL). Independent 5-lens review: **FAIL** (2 blockers, 12 majors) → remediated (added ADR-0015 testable engine, ADR-0016 decode sandbox, UX-CANONICAL) → re-review **PASS WITH CONDITIONS** (both blockers cleared, 12/14 majors) → remaining M2/M11/NM-1/NM-3 closed. **Figma designs** added to file `SYQn5hFY8YVQKm3c6rw0eJ` (user refine, expanded to full app): **19 frames** — Operator Console, Mobile Controller, Stage/Confidence output, TIME UP output, Design Tokens, Scripture (search & present), Presentation/Slide Editor, Theme/Template Designer, Song/Lyrics Editor, Media & Library, Service Plan builder, Settings (AI & providers), Add Bible (translation import), Displays & Outputs, Devices & Roles (pairing + RBAC), Pre-service Checks, Transcription/STT, Scripture Detection queue, Sermon Notes. **Refine (user):** Operator Console unified into one work screen — added Plan/Scripture tabs + a bottom work dock (live transcript/STT + scripture search & auto-detect approval); scripture search & display added to the Mobile Controller. PRD validator passes. Awaiting Stage 5 gate. Next: **Stage 6 — ClickUp delivery planning** (gate to begin implementation).

## Key artefacts

- Baseline audit: `docs/product/audits/BASELINE.md`
- Research plan: `docs/research/RESEARCH-PLAN.md`
- Risk register: `docs/delivery/RISK-REGISTER.md`
- Goal Contract validator: `scripts/validate_goal_contract.py`
- Stage 1 Goal Contract: `docs/delivery/goals/STAGE1-bootstrap.md`

## Recovery note

Greenfield build; not yet under git version control (RISK-011). To resume, read the Build Control task and this file, validate the BUILD + active STAGE contracts, then resume at the last unapproved gate (currently the Stage 1 gate).

## Stage 6 outcome (2026-07-23)

ClickUp delivery plan created in list SelahCue — Delivery (901327960792): **16 epics** (11 MVP + 5 later-release) + **14 foundation vertical-slice stories** + **3 milestones**. Requirement traceability (`docs/delivery/REQUIREMENTS-TRACEABILITY.md`): all 204 FR+NFR mapped. `docs/delivery/IMPLEMENTATION-READINESS.md`: **READY** with a 37-edge acyclic dependency graph + critical path. New validator `scripts/validate_delivery_plan.py` (coverage + acyclicity + READY) passes. Independent review (`DELIVERY-PLAN-REVIEW.md`): PASS WITH CONDITIONS, 0 blockers; medium/low traceability nits fixed. Awaiting Stage 6 gate — **this gate authorises Stage 7 (production implementation)**.

## Stage 7 outcome — batch 7a (2026-07-23)

First production code: `implementation/crates/selahcue-core` (scripture parser FR-027, service-plan model FR-001/002, monotonic timer FR-054/065). **Verified: cargo test 37/37, clippy clean.** Independent review PASS WITH CONDITIONS → all findings fixed. Stories 86ajp0afa/86ajp0a4z/86ajp0ac9 in progress. Git e794a7e. Next batches: walking skeleton (wgpu+Tauri, needs display), persistence (SQLite/SQLCipher), CI/GPU matrix, presentation/outputs/mobile → foundation demo.

## Stage 7 outcome — batch 7b (2026-07-23)

Persistence layer: `implementation/desktop/crates/selahcue-data` — WAL SQLite (rusqlite bundled), versioned append-only migrations + forward-compat guard, integrity checks, crash-safe online backup (FR-079), transactional `ServicePlan` repository. **Verified: cargo test 50/50, clippy clean.** Independent review (`implementation/desktop/CODE-REVIEW-batch7b.md`) PASS WITH CONDITIONS (0 high, 1 med, 3 low) → M1/L1/L2 fixed with regression tests. **Repo restructured** by platform: `implementation/{desktop,web,mobile}/` (user refine — separation of concerns). Git 4e6612a.

## Stage 7 outcome — batch 7c (2026-07-23)

At-rest encryption (FR-154): `selahcue-data` `encryption` feature (SQLCipher + vendored OpenSSL, self-contained); `EncryptionKey` (256-bit raw key, zeroize), `open_encrypted`/`open_in_memory_encrypted`, and crash-safe `backup_to_encrypted`. Keyed-open API is feature-gated so a keyless "encrypted" open is impossible. **Verified: cargo test 50 default + 16 data-encryption, clippy clean both; no plaintext on disk (header + WAL encrypted).** Independent **multi-lens adversarial review** (workflow, 4 lenses find→verify): 8 raised → **2 confirmed** (H1 encrypted-backup-broken, L1 key-lingers) → fixed + regression-tested; 6 dismissed (`CODE-REVIEW-batch7c-encryption.md`). **Tests reorganised** into per-crate `tests/` folders (user refine; one white-box scripture test stays inline). Story 86ajp09he → **QA** (data-layer scope); OS secret-store key acquisition (app-shell, ADR-0007) deferred to follow-up story 86ajp5vp6. Batch-7a stories (scripture/plan/timer) → QA. Next batches: walking skeleton (wgpu+Tauri, needs display), CI/GPU matrix, presentation/outputs/mobile → foundation demo.

## Stage 7 outcome — batch 7d (2026-07-23)

LAN control core: new crate `implementation/desktop/crates/selahcue-lan` (FR-118/119/120; ADR-0009) — wire **protocol** (versioned JSON, stable tags, `version_supported`), **RBAC** (Operator>Producer>Assistant>Viewer, single `authorize()` choke point, strict-superset roles), **session/pairing** (single-use TTL codes → opaque bearer tokens; constant-time auth via `subtle`; redacted-Debug tokens; no cross-device confusion). Pure + injected token/clock. **Verified: cargo test 80/80 workspace (30 in lan), clippy clean.** Independent **multi-lens adversarial review** (workflow): 10 raised → **1 confirmed** (M1 AuthRequest Debug leaked token) → fixed + regression test; 9 dismissed; empty-token/code guards + cross-device/boundary tests added (`CODE-REVIEW-batch7d-lan.md`). Story 86ajp0b0t → **in progress** (RBAC+pairing core done; TLS transport = batch 7e, QR+mobile client pending). **Design question resolved:** Assistant keeps `Clear`/`Navigate` on the live output ([DEC-002](../decisions/DECISION-LOG.md), user "keep as is"). Next batch: **7e — TLS-pinned WebSocket transport**.

## Stage 7 outcome — batch 7e (2026-07-23)

TLS-pinned WebSocket transport: `selahcue-lan` `server` feature (pure-Rust rustls + ring — no OpenSSL/C). `pinning` (custom rustls verifier: trusts the operator's self-signed cert by SHA-256 pin while still verifying the handshake signature), `tls` (rcgen self-signed + server/client configs), `server`/`client`/`wire`. **E2E over real loopback TLS:** pinned + authenticated + RBAC-enforced command flow; wrong pin rejected; wrong token rejected; half-open (slowloris) connections reaped; connection/registry bounded. **Verified: cargo test 84 default + 39 server-feature, clippy clean both.** Independent **multi-lens adversarial review**: TLS-security lens **clean (no bypass)**; 8 raised → **4 confirmed** — all pre-auth DoS/resource (accept-loop-dies-on-error, no handshake-timeout/connection-cap ×2, 64 MiB frame) → **fixed** with handshake timeout + `Semaphore` cap + resilient accept + 64 KiB frame cap + slowloris-reaping regression test (`CODE-REVIEW-batch7e-transport.md`). **No-leak requirement** ([memory note] the user added): bounded `SessionRegistry` tests + connection-reaping test. Story 86ajp0b0t stays **in progress** (transport done; QR pairing + Flutter mobile client pending). Also: runnable QA demo added (`cargo run -p selahcue-lan --example demo --features server`).

## Stage 7 outcome — batch 7f (2026-07-23)

Testable render-engine seam (ADR-0015): new crate `implementation/desktop/crates/selahcue-engine` — `scene` (backend-independent Frame model, serde), `raster` (GPU-free deterministic rasterizer + pixel readback), `fault` + `engine` (never-blank `Engine`: InjectFault holds the last good frame for all 4 fault kinds + cross-output isolation + recovery; versioned render↔control IPC `EngineCommand`/`EngineEvent`), `analysis` (FR-175 flash analyzer + NFR-004 latency proxy). **Verified: cargo test 28 engine / 112 workspace, clippy clean.** Independent **multi-lens adversarial review** (reliability · raster · **analysis-validity** · api-memory): 7 raised → **4 confirmed** — two safety-critical flash false-passes (whole-capture average H, whole-frame-mean localized M), a /2-truncation boundary (M), and an unvalidated-dimension crash defeating never-blank (H) → **all fixed** (worst-1s-window + per-tile + fractional flash counting; frame-dimension validation with reject-and-hold) + regression tests (`CODE-REVIEW-batch7f-engine.md`). Story **86ajp09m9 in progress** — the 3 acceptance criteria are met by the CPU seam; wgpu-shared on-screen path + cross-GPU SSIM parity + GPU CI matrix pending (walking skeleton / CI batches). This seam **unblocks** Outputs/Timers/Media/Reliability stories.

## Stage 7 outcome — batch 7g (2026-07-23)

Basic presentation rendering: new crate `implementation/desktop/crates/selahcue-present` (FR-009/012/013/036/046), built on the 7f engine seam. `slide` (Slide + audience Theme, serde), `compose` (slide → engine Frame, safe-area layout), `present` (`Presenter`: separate Preview + Live engine outputs enforcing **staging never changes Live until Go Live**; clear = Esc Esc, blackout toggle+restore = B; offscreen preview readback). **Verified: cargo test 18 crate / 130 workspace, clippy clean** — preview/live isolation, go-live push, **≤150ms** slide-trigger (real wall-clock at 1080p), un-blackout restores, flash-safe sequence, bounded state (no-leak). Independent **multi-lens adversarial review** (invariant · composition · latency-fidelity · api-memory): 6 raised → **4 confirmed** (dimension-clamp/tracked-state truthfulness, bottom safe margin, tautological latency test) → **fixed** + regression tests; blackout-vs-go-live dismissed as matching UX-STATE-MATRIX/FR-077 (`CODE-REVIEW-batch7g-present.md`). Story **86ajp0a6q in progress** — presentation logic + preview/live isolation + latency met; the operator chrome + fullscreen window are the walking-skeleton batch.

## Stage 7 outcome — batch 7h (2026-07-23)

Stage/confidence display output: added `stage` module to `implementation/desktop/crates/selahcue-present` (FR-037/040) composing the timer core + presenter. `TimerView` (from `Timer`: ok/warn/TIME-UP + progress), `compose_stage` (timer bar with state-coloured progress fill + TIME-UP full-red, current + next line regions, clock strip), `compose_identify` (a distinct number per physical display), `StageDisplay` (second output surface). Added `Presenter::identify` (main-output identify) and refactored `compose.rs` to share `layout_lines` (compose_slide kept **pixel-identical**). **Verified: cargo test 26 crate / 138 workspace, clippy clean** — timer state→colour, **main vs stage show different content from one live state** (acceptance), identify distinctness, bounded state (no-leak). Independent **multi-lens adversarial review** (output · timer-view · compose-refactor-regression · api-memory/identify): **0 findings** — the regression lens confirmed no compose_slide pixel drift (`CODE-REVIEW-batch7h-stage.md`). Story **86ajp0aa4 in progress** — stage composition + identify logic met; the second native output window + physical-display enumeration need the walking skeleton. **Foundation now spans 8 verified batches (7a–7h): domain core, persistence (+encryption), LAN control + TLS transport, render-engine seam, presentation Preview→Live, and stage/confidence output — 138 tests + 55 feature-gated, clippy clean, each independently reviewed.** ## Stage 7 outcome — batch 7i (2026-07-23)

Walking skeleton (render half): new crates `selahcue-gpu` (wgpu compositor rendering the engine scene: instanced `Layer::Fill` rects, background, blackout, to an offscreen texture + pixel readback) and `selahcue-desktop` (native borderless output window — winit + wgpu surface — presenting the presenter's live output). Engine gained `FrameBuffer::from_rgba` + `analysis::ssim` (per-channel, the ADR-0015 parity oracle). **GPU is present here (Metal, Apple M5): the offscreen compositor is RUNTIME-VERIFIED against the CPU rasterizer at SSIM ≥ 0.99** across opaque/translucent/overlap/blackout/non-aligned-width scenes — **closing the ADR-0015 cross-GPU parity gap** from 7f. The on-screen window is **compile-verified only** (no display), runnable on a Mac via `cargo run -p selahcue-desktop`. **Verified: cargo test 141 workspace, clippy clean.** Independent **multi-lens adversarial review**: 11 raised → **6 confirmed** — the parity-fidelity lens found the SSIM oracle would pass a *broken* compositor (never exercised alpha/row-padding; luminance-only) → **fixed** (per-channel SSIM + translucent + non-64-width parity scenes) + desktop surface-loss-freeze + case-sensitive blackout key fixed (`CODE-REVIEW-batch7i-gpu.md`). Story **86ajp09c2 in progress** — Rust core + wgpu compositor + native output window delivered; the **Tauri operator shell** is the next slice. **Foundation now: 9 verified batches (7a–7i), 141 tests + 55 feature-gated + GPU parity, clippy clean, each independently adversarially reviewed.** ## Stage 7 outcome — batch 7j (2026-07-23)

Glyph text rendering (FR-009): added `Layer::Text` to the engine scene + a CPU glyph rasterizer using bundled public-domain **font8x8** (LSB-first, verified non-mirrored, clipped to rect). The presentation `compose` now emits `Layer::Text` (real glyphs) instead of placeholder bars, so the **desktop output window renders legible text** (user-confirmed on Mac). The wgpu rect compositor skips Text (GPU-native glyphs are a future ADR-0002 slice); the on-screen path CPU-composites via `selahcue-present` + blits. **Verified: cargo test 145 workspace, clippy clean; GPU parity intact (Fill-only).** Independent **multi-lens adversarial review**: 4 raised → **2 confirmed** — `draw_text` did unbounded work for an oversized px/rect (CPU-exhaustion hazard) → **fixed** (scale cap + framebuffer-clipped block loop + regression test); a stale compositor doc claiming the rect pipeline drives the screen → corrected (`CODE-REVIEW-batch7j-text.md`). Story **86ajp0a6q** — "text over background" now renders real glyphs. **Foundation: 10 verified batches (7a–7j), 145 tests + 55 feature-gated + GPU parity, clippy clean, each independently adversarially reviewed.** ## Stage 7 outcome — batch 7k (2026-07-24)

LAN control → presenter: new crate `implementation/desktop/crates/selahcue-app` — `LiveController` maps RBAC-checked control commands onto the presenter + service plan (Next/Previous/SelectItem stage Preview; GoLive commits; Clear/Blackout act on Live; ScriptureSearch parses; GetState reports), and `handler_for` wires it into the pinned-TLS `ControlServer` (added `Reply::Deny` to lan). **The complete remote-control loop is verified end-to-end** (`--features server`): a Producer client over real TLS drives the live output (Next→preview, GoLive→live, Blackout→black); Assistant GoLive denied by RBAC; unknown item → BadRequest. **Verified: cargo test 155 workspace (10 app unit + 2 E2E), lan server 39, clippy clean.** Independent **multi-lens adversarial review**: 4 raised → **4 confirmed** — staged-scripture-can't-go-live (×2) + scripture-resets-plan-nav → **fixed** (GoLive off presenter state; dedicated `plan_cursor`) + regression tests; **1 HIGH RBAC finding routed to owner** — `Clear`→`Navigate` lets an Assistant wipe Live + lift blackout, which is the policy kept in DEC-002, now surfaced for revision (`CODE-REVIEW-batch7k-app.md`). Story 86ajp0b0t — the control-drives-presenter loop is done (QR/mobile client + walking-skeleton wiring remain). **Foundation: 11 verified batches (7a–7k), 155 tests + E2E + GPU parity, clippy clean, each independently adversarially reviewed.**

**Refine (2026-07-24):** the batch-7k HIGH RBAC finding was routed to the user, who chose "tighten it." `Command::Clear` moved from `Navigate` to a dedicated `Permission::ClearLive` (Operator + Producer only) — an Assistant can navigate/stage Preview but can no longer wipe the live output or lift blackout. [DEC-002](../decisions/DECISION-LOG.md) → REVISED. Verified: unit rbac denials + an E2E Assistant-`Clear`-denied assertion over the wire; a focused adversarial re-verification found 0 defects. lan 34 default + 39 server, app 10 + 2 E2E, workspace 155, clippy clean. Next: operator shell (Tauri), timers-on-output, mobile client, or CI.

## Stage 7 outcome — batch 7l (2026-07-24)

Desktop output window ← remote control: rewired `selahcue-desktop` (`selahcue-output`) to run the pinned-TLS `ControlServer` + a shared `Arc<Mutex<LiveController>>` on a background Tokio thread, so a **remote controller drives the on-screen output** (Next/GoLive/Blackout/Clear), while local keys drive the *same* controller. Continuous vsync-paced repaint (`about_to_wait`) so remote-only changes (not winit events) appear within a frame; window reads `presenter().live_output()`. Added `CertPin::from_hex` (+ unit test) and a lean **`selahcue-remote` CLI** (a `selahcue-lan` example, off the wgpu graph) so the window can be driven from another terminal; the window prints the exact address/pin/device/token on startup. Server binds **loopback only** with a fixed demo token + pre-paired Producer (real LAN + QR/host-confirmation pairing is the mobile-client batch). **Verified: cargo test 155 workspace; lan 40 server (+1 `from_hex`); app 10 + 2 E2E; clippy clean incl. the new example; desktop builds clean.** The on-screen window is **compile-verified only** (no display); the control loop it packages is E2E-verified in `selahcue-app`; runnable on the user's Mac. Independent **multi-lens adversarial review** (concurrency · wiring · parser/CLI · security; workflow `wag2rpohk`, 8 agents): **4 raised → 0 confirmed** (all 4 adversarially dismissed) — one acknowledged non-blocking nit (UI mutex held across the vsync present) recorded as verified-immaterial, no fix warranted (`CODE-REVIEW-batch7l-desktop.md`). Story **86ajp09c2** (walking skeleton) — Rust core + wgpu window + on-screen text + **remote-driven output** delivered; the **Tauri operator shell** remains. **Foundation: 12 verified batches (7a–7l) + RBAC refine, 155 tests + E2E + GPU parity, clippy clean, each independently adversarially reviewed.** Next: Tauri operator shell, timers-on-output, mobile (Flutter) client, or CI/GPU matrix.

**ClickUp brought current (2026-07-24):** user flagged that the BUILD CONTROL task (`86ajnx548`) was stale (description said "Stage 1 ← current"; gate-history comments stopped at 7a). Updated its description to Stage 7 (7a–7l), set status → in progress, and posted a full 7a–7l batch-log comment. Standing rule saved to memory: update the control task every batch, not just sub-stories.

## Stage 7 outcome — batch 7m (2026-07-24)

Operator shell (the last slice of the walking-skeleton story bar full wiring). Two parts: (1) a **verified, UI-agnostic operator surface** in `selahcue-app` — `OperatorView`/`ItemView` (serde) + `OperatorShell` (ergonomic wrapper over the shared `Arc<Mutex<LiveController>>`; each action applies then returns a fresh `OperatorView`) + `LiveController::operator_view()` — with **8 unit tests** (initial/next/go-live/advance/select/blackout/clear/serialize/clone-shares-controller). (2) a **Tauri 2 desktop app** `selahcue-operator` — `#[tauri::command]`s (view/next/previous/go_live/clear/blackout/select) over the shell + a **self-contained dark webview console** (plan list with LIVE/PREVIEW badges, Prev/Next/GO LIVE/Blackout/Clear); `withGlobalTauri`, no npm/bundler. **The Tauri app is EXCLUDED from the default workspace** (a GUI that can't be runtime-verified headless + a heavy toolchain tree) but was **compile-checked (`cargo check`) + clippy clean** standalone (the whole Tauri/wry tree built on macOS; needed a generated `icons/icon.png`). **Verified: workspace 171 tests (+8 operator); operator `cargo check` + clippy clean; app clippy clean.** Independent **multi-lens adversarial review** (view-model · shell-concurrency · tauri-frontend · integration-honesty; workflow `w4d8xq56o`, 7 agents): **3 raised → 1 confirmed → fixed** — all three named the *same* docstring; the operator *logic* (view-model, concurrency, Tauri wiring, frontend, security config) drew **0 findings**. The confirmed one: the `OperatorShell` docstring overclaimed that the output window + operator "drive one live state" (an integration not yet wired) → **fixed** to a qualified capability statement (`CODE-REVIEW-batch7m-operator.md`). **Honest boundary:** the Tauri shell drives its **own** in-process controller; it is **not yet connected** to the batch-7l on-screen output window — that operator↔output integration is the next slice. Story **86ajp09c2** (walking skeleton): Rust core + wgpu output window + on-screen text + remote-driven output + **operator UI shell** delivered; the operator↔output wiring (shared core or control-client) is the remaining integration. **Foundation: 13 verified batches (7a–7m) + RBAC refine, 171 tests + E2E + GPU parity, clippy clean, each independently adversarially reviewed.** Next: wire the operator shell to the output window (shared core), timers-on-output, mobile (Flutter) client, or CI/GPU matrix.

## Stage 7 outcome — batch 7n (2026-07-24)

Operator ↔ output-window wiring (closes the 7m seam) via **option B: the operator is a control client of the output window**, which owns the one authoritative controller. Extended the LAN protocol (`Command::GetOperatorState` + `ServerMessage::OperatorState{OperatorStateView}`; RBAC Monitor). Added **`RemoteOperator`** to `selahcue-app` (`server` feature): a pinned-TLS control client whose actions drive the host's controller and read back its operator view (with app↔wire `From` conversions so the same `OperatorView` renders local or remote). The **output window** writes a local endpoint descriptor (addr/pin/device/token; token chmod 0600 on unix, per-user `$TMPDIR` on macOS; removed on clean exit); the **Tauri operator app** auto-connects to it (`Backend::Remote`) or falls back to a standalone demo (`Backend::Local`), so **operating the Tauri UI now changes the on-screen audience output**. Added a repo-root **`Makefile`** (`make launch` runs the output window + operator shell together; plus output/operator/remote/demo/test/check/clippy). **Verified: workspace 173 tests; app server 8 operator + 2 operator-remote E2E + 2 remote E2E; lan 40 server; operator `cargo check` + clippy clean; desktop builds.** The operator↔output loop is verified **headlessly** (the E2E host controller stands in for the one the output window renders); the GUI + on-screen rendering are compile/prior-verified; runnable on a Mac via `make launch`. Independent **multi-lens adversarial review** (protocol-rbac · conversion · remote-client · integration-security; workflow `wwzeesec0`, 8 agents): **4 raised → 1 confirmed → fixed** — `ControlClient::connect` had no timeout, so under the Tauri `block_on` startup a stale endpoint pointing at an accept-but-stall peer could hang the app window-less; **fixed** with a client `CONNECT_TIMEOUT` mirroring the server + endpoint cleanup on exit (`CODE-REVIEW-batch7n-wiring.md`). 3 dismissed (Viewer-reads-plan = within Monitor scope; silent Local fallback = disclosed; /tmp path = mitigated). Story **86ajp09c2** — the walking skeleton is now **end-to-end**: operator UI → control client → host controller → audience output window. **Foundation: 14 verified batches (7a–7n) + RBAC refine, 173 tests + E2E + GPU parity, clippy clean, each independently adversarially reviewed.** Next: timers-on-output, mobile (Flutter) client + QR pairing, or CI/GPU matrix.

## Stage 7 outcome — batch 7o (2026-07-24)

Timers on the output. Wired `StartTimer`/`StopTimer` (previously no-ops) to a real countdown in the `LiveController`: `apply` creates a `Timer::count_down` (pending), and a new **injected-clock `tick(now)`** starts it and overlays it on the Live output via `Presenter::show_timer`; the desktop render loop ticks each frame so the countdown runs on-screen (green → amber warn → red **TIME UP**, with `M:SS` text). **Safety-critical:** blackout lives in `scene.blackout` and `SetScene` replaces the scene, so the presenter now tracks `live_blackout` and stamps it on every timer recompose — an operator blackout survives the tick (dedicated test). Recompose is keyed on the *displayed* value (`timer_display_key`), so it runs ~1/sec, and overrun sits idle. Added `TimerSnapshot` to the operator view (`OperatorStateView`), `start_timer`/`stop_timer` on `RemoteOperator`/`OperatorShell`, a CLI `timer`/`stop-timer` command (+ `make remote CMD=timer SECS=`), and Tauri Start/Stop buttons + a 1s poll. **Verified: workspace 181 tests** (present overlay + **blackout-preserved-across-recompose**; controller lifecycle + **ceil** + **restart** regressions; a **remote start_timer E2E**); operator `cargo check` + clippy clean; desktop builds. Independent **multi-lens adversarial review** (timer-correctness · **blackout-invariant** · integration-staleness · safety-memory; workflow `wgymksqae`, 7 agents): **3 raised → 2 confirmed → fixed** — countdown floored instead of ceiled (start value flashed, "0:00" for the whole final second) → **ceil**; restarting a running timer reported a stale snapshot → **reset `last_timer_view` on `StartTimer`**; 1 dismissed (overrun re-rasterize) improved anyway. The blackout-invariant lens found **nothing** (`CODE-REVIEW-batch7o-timers.md`). **Foundation: 15 verified batches (7a–7o) + RBAC refine, 181 tests + E2E + GPU parity, clippy clean, each independently adversarially reviewed.** Next: mobile (Flutter) client + QR pairing, CI/GPU matrix (cross-OS launch + NFRs), or the stage/confidence output window.

## Stage 7 outcome — batch 7p (2026-07-24)

Stage/confidence second window. The `LiveController` now owns a `StageDisplay` and refreshes it (gated to ~1/sec via a dirty flag + timer-key change) from the same live state — composing the speaker view (current line + next line + timer + clock, FR-037) — and exposes `stage_output()`. The **desktop opens two windows** ("SelahCue Output" + "SelahCue Stage / Confidence"), each rendering its own wgpu surface from the one shared controller, dispatched by `WindowId`. Added a repo-root `make timer`/`make stop-timer`. **Refine (user):** the countdown is a speaker aid, so it now renders **only on the stage/confidence monitor**, not the audience/program output — removed the entire live-timer overlay (`show_timer`/`recompose_live`/`compose_live`/`timer_bar_layers`) and reverted the audience `go_live`/`clear`/`blackout` behavior; the timer still flows to the `StageDisplay` + the operator view (operators/CLI still trigger + see it). **Verified: workspace 181 tests** (stage is a distinct non-blank surface; the audience output never shows a timer; the monitor shows it green/red); clippy clean (present/app/desktop/operator); both binaries build. **Two independent adversarial review passes:** pass 1 (`we7hbankn`) — 1 confirmed (the two-window continuous-redraw busy-spun a CPU core when a window can't present) → **fixed** (deadline-paced loop); pass 2 (`wt28silw6`, post-refine) — 1 confirmed (continuous input starved the naive paced loop) → **fixed** (a **fixed-schedule** `next_frame` deadline); the timer-placement lens found nothing (`CODE-REVIEW-batch7p-stage.md`). **Honest scope:** the two windows + on-screen rendering are compile-verified (headless env); the stage/timer logic is unit-tested + the paced loop adversarially re-verified. Story **86ajp0aa4** (stage/confidence display) — the composition (7h) is now on a real second window. **Foundation: 16 verified batches (7a–7p) + RBAC refine + timer→stage refine, 181 tests + E2E + GPU parity, clippy clean, each independently adversarially reviewed.** Next: mobile (Flutter) client + QR pairing, CI/GPU matrix, or physical-display enumeration/identify.

**Hotfix (2026-07-24, user QA):** "the timer does not show up on the confidence monitor" — the stage timer strip was only the 7h-era progress fill (no numeric readout; idle full-green bar mistakable for a running timer). `compose_stage` now takes `Option<TimerView>`: a running timer renders a large `M:SS`/`TIME UP` glyph readout; idle shows an empty dim track. Regression tests added; workspace 183; commit `582d9dc`.

## Stage 7 outcome — batch 7q (2026-07-24)

**Mobile pairing (QR + host confirmation) + the Flutter controller.** Wire pairing: protocol **v2** — the first frame is a tagged `Hello{auth|pair}`; `complete_pairing` validates the single-use TTL code (non-consuming pre-check so bad codes never prompt), requires **host confirmation** (`PairingApproval` async seam, 30s window separate from the 10s slowloris timeout), then redeems with **server-generated credentials** (ring); the connection continues authenticated. Secure default: pairing rejected unless opted in. **6 E2E tests over real TLS**: pair→control→reconnect, decline, unknown-code-no-prompt, disabled-by-default, single-use replay, expiry. `PairingInvite` URI (`selahcue://pair?host&port&pin&code`) + `qr.rs` (qrcode → engine frame) rendered on the **stage** output (audience never shows pairing) + ASCII terminal QR. Desktop: **P** offers pairing (2min TTL, prune, code **withdrawn on cancel**), **Y/N** confirms (prompt discloses the **Producer** grant; a timed-out prompt self-heals), binds **0.0.0.0** with a per-run **random** operator token; CLI `pair <uri>`. **Flutter controller** (`implementation/mobile/selahcue_controller`, **MVC per user refine**: models/controllers/views): pinned-TLS session (`withTrustedRoots:false` + SHA-256 pin), scan (mobile_scanner) or paste pairing, keystore credential storage, plan list + GO LIVE/blackout/clear/timer + 1s in-flight-guarded poll, leak-safe reconnect. **Cross-language wire contract byte-pinned** in both test suites. `make mobile`/`mobile-test`; READMEs updated. **Verified: Rust workspace 198 (lan server 50); clippy clean; `flutter analyze` clean; 12 Dart tests; `flutter build macos` succeeds.** Independent **multi-lens adversarial review** (5 lenses, 26 agents, `wvg07j8r0`): **21 raised → 21 confirmed → 11 unique defects → all fixed** (approval-slot wedge; withdraw-on-cancel; Dart socket leaks; poll backlog; stale-reply handling; prune wiring; loopback warning; Producer disclosure; symmetric fixtures; doc drift) + an adversarial **re-verification pass** of the fixes (`CODE-REVIEW-batch7q-pairing.md`). **Honest scope:** pairing E2E-verified over real TLS; the app analyze/test/macOS-build-verified, **not yet run on a physical phone** (user QA: `make output` → P → `make mobile` → scan → Y); mDNS deferred. Story **86ajp0b0t** — QR pairing + pinned TLS + RBAC + the Flutter client delivered; on-device phone QA + mDNS + per-offer role choice remain. **Foundation: 17 verified batches (7a–7q) + 3 refines, 198 Rust tests + 12 Dart tests + E2E + GPU parity + a macOS app build, each batch independently adversarially reviewed.**

## Stage 7 outcome — batch 7r (2026-07-24)

CI + test infrastructure. Authored **`.github/workflows/ci.yml`**: `rust` job on a **Linux/macOS/Windows matrix** (fmt gate, clippy `-D warnings` incl. server-feature combos, full workspace tests, LAN/app E2E, encryption on ubuntu+macos), `operator` (Tauri) check on 3 OSes with the Linux webkit deps, `flutter` analyze+test, and a **RustSec audit** job; PR-runs-only cancellation so every main commit keeps a verdict. **GPU matrix:** the ADR-0015 parity tests run wherever an adapter exists (mesa/lavapipe installed on ubuntu so they execute there) and skip gracefully otherwise (verified). One-time **`cargo fmt` normalization** of both Rust workspaces (tests + clippy re-verified green) so the fmt gate is enforceable. **`make ci`** runs the same gate locally — **executed here: ALL GREEN**. **`make nfr`** measures the walking-skeleton NFRs on the release build with a crash-safe harness — **executed here: cold start 1.13s (≤3s) PASS, idle memory 121.5MB (≤300MB) PASS** (Darwin arm64). **Hosting (user decisions):** the user uses GitLab, so a Free-tier-scoped `.gitlab-ci.yml` was authored and compared honestly (GitLab Free has **no macOS runners**, bare-beta Windows, 400 min/mo); on the recommendation the user chose **GitHub** (full free 3-OS matrix) — the GitLab variant was removed (git history retains it). Independent **adversarial review** (`wdbqc81qc`, 8 agents): 5 raised → **4 confirmed → fixed** — headline: the NFR harness could report a **fabricated PASS (0.0MB)** if the app crashed mid-measurement (found by two lenses, one reproduced it live) → liveness-checked sampling + captured crash logs + zero-samples-FAIL; also main-push cancellation and a stale endpoint file (`CODE-REVIEW-batch7r-ci.md`). **Honest boundary:** the workflow is authored + validated + local-parity-verified but has **not executed on hosted runners** — no remote/credentials exist here; activation = the user creates the private GitHub repo + pushes (exact steps at the gate). Story **86ajp09nk** stays in progress pending that activation. **Foundation: 18 batches (7a–7r) + 3 refines.**

## Stage 7 outcome — batch 7s (2026-07-24)

**CI-run stabilization — the hosted matrix is green.** The user created + pushed **github.com/First-Pavilion/selahcue** (private). First run: 4/7 jobs green immediately (rust macos, operator ubuntu, flutter, audit). The 3 red jobs reduced to **2 root causes**, both fixed (`6ef8d9e`): (1) the slide-trigger latency test enforced the 150ms **release** NFR on debug builds riding shared runners (184/207ms observed) → debug builds now carry a 1.5s regression tripwire while **release builds enforce 150ms**, additionally run by `make nfr` on real hardware (passes in 0.01s); (2) `tauri-build` on Windows requires `icons/icon.ico` → generated a multi-size BMP-format ICO + tauri.conf wiring. **Re-run verdict, independently confirmed via `gh` (run 30077797265): all 8 jobs green** — rust ×3 OS, operator ×3 OS, flutter, audit. **The ADR-0015 GPU-parity test EXECUTED and passed on all three backends** per the runner logs: ubuntu **Vulkan/lavapipe** 3.43s, macOS **Metal** 0.65s, Windows **DX12/WARP** 2.02s — continuous three-backend parity evidence. An independent ubuntu:24.04 container reproduction separately validated the Linux dep list + vendored-OpenSSL build. `gh` CLI now authenticated locally (user), so run verdicts are self-serve going forward. Verification note: the fixes were verified by the hosted matrix itself (an independent execution environment) + local suites; a further agent-review of two empirically-validated fixes was judged redundant. Story **86ajp09nk → QA**. **Foundation: 19 batches (7a–7s) + 3 refines — every push now runs the full 3-OS gate.**

## Stage 7 outcome — batch 7t (2026-07-24)

**Foundation-demo milestone review** (`86ajp0bpn`) — a live demo walk on the release binary over the real control link (state-recorded), producing `docs/delivery/FOUNDATION-DEMO-REVIEW.md`. The review doc was itself **adversarially audited** (claims-vs-evidence, 2 agents, cross-checking code/tests/CI logs/live ClickUp): **17 findings — all corrected**, incl. my own overclaims (scripture "remote E2E" that didn't exist; a pairing chain proven only against a stub handler; "transaction-tested"; embellished GPU-parity/adversarial-review/QR wording) — plus **1 new E2E added to close a gap with code**: `wire_paired_device_advances_the_live_output` (pair over the wire → the real `LiveController`'s live output changes; workspace now 199). **Verdicts: 1 PASS · 3 PASS(scoped) · 4 PARTIAL · 1 FAIL** (force-kill recovery — story `86ajp09td` never started; live-recorded `Some(3)`→kill→`Some(1)`). **Milestone: NOT met — stays open.** The review also surfaced a **ClickUp reconciliation table**: 5 stories closed complete whose acceptance text includes undelivered items (cross-OS launch; persistence binding + library scope; bundled PD translations/verse text; per-display identify; SBOM/license/device-loss CI items) — tabled for owner disposition (descope decisions or follow-up stories) at the gate. Dependabot #1 (glib unsoundness) accepted + tracked as RISK-015 (Tauri-blocked upstream; low residual). **Foundation: 20 batches (7a–7t) + 3 refines.** Next: close gap 1 (autosave/recovery) + gap 2 (plan wiring), or owner-directed.

## Stage 7 outcome — batch 7u (2026-07-24)

**Autosave + crash recovery — demo step 9 FAIL → PASS.** Migration v2 (`session_state` singleton) + **v3** (scripture references); `session_repo` (UPSERT autosave, corruption-surfacing loads, FK SET-NULL tested); `Timer::with_elapsed` (a recovered countdown resumes; deep overrun lands in TIME UP); `ControllerSnapshot`/`snapshot(now)`/`restore()` rebuilt through the command paths (byte-identical outputs, blackout returns dark, stale indices safe); `Presenter::clear_preview`; desktop `SessionStore` at the platform data dir (in-memory degradation always reported), throttled autosave (≥1s; 5s timer refresh; **retry on failed writes**), clean-exit save. Also this batch (reconciliation, user-directed): **5 follow-up stories created** (86ajpevzp cross-OS launch/NFR, 86ajpew05 scripture verse-text, 86ajpew0j SBOM/license/device-loss CI, 86ajpew01 plan wiring + library, 86ajpew0c display enumeration/identify) and linked from the closed stories. **Review** (13 agents, resumed across a session restart, one finding empirically reproduced): 10 raised → 9 confirmed → **6 unique defects → all fixed** — phantom staged preview after restore; scripture-on-LIVE recovering blank (→ schema v3 + reference tracking); failed-autosave dirty-flag loss; silent corrupt-session discard; silent storage degradation; XDG empty/relative handling (`CODE-REVIEW-batch7u-recovery.md`). **Verified: workspace 212; live `kill -9` walk restores `Some(2)`+blackout identically ("Session restored"); the user's real v2 store migrated to v3 in place and restored across builds (1 plan row — no orphan growth) — including through real user keystrokes + a clean-exit save.** Story `86ajp09td` stays in progress for the crash-loop breaker + storage guard. **Foundation: 21 batches (7a–7u) + 3 refines; demo score now 2 PASS · 3 PASS(scoped) · 4 PARTIAL · 0 FAIL.**

## Stage 7 outcome — batch 7v (2026-07-24)

**Plan authoring + persistence wiring + library — demo step 2 PARTIAL → PASS(scoped).** Four plan-edit wire commands (`AddItem`/`RemoveItem`/`MoveItem`/`RenameItem`) behind a new **Operator-only** `Permission::EditPlan` (a Producer's edit is RBAC-denied — wire-tested end-to-end); `LiveController` edit arms with full index fixup (removing the LIVE item keeps the slide on the audience output **and across crash recovery** — the removed item's text is tracked as a free live slide); `plan_repo::update` (transactional re-write) / `search` / `duplicate` — library search **<300ms at 5k plans** (perf-guarded); desktop wiring: plan edits persist immediately with a **joint plan+session write** (throttle bypassed so on-disk plan+indices never desync), failed writes retried, clean exit flushes pending edits, first-run seed writes its session row synchronously (no orphan rows); operator shell edits **in-page** (native `prompt()`/`confirm()` are unimplemented in wry/WKWebView — verified in vendored source): inline rename, two-click delete, add-item row, poll-safe rendering. **Review** (run `wcdpt578m`, multi-lens + adversarial verification): 14 raised → 14 confirmed → **9 unique defects (A–I) → all fixed**, incl. the dead-dialog UI, the stale-index crash window, a raw Operator token printed to stdout, and LIKE-wildcard injection in search (`CODE-REVIEW-batch7v.md`). **Verified: workspace 220 (+2 regressions), clippy clean, operator crate clean.** Story `86ajpew01` delivered (plan wiring + library). **Foundation: 22 batches (7a–7v) + 3 refines; demo score now 2 PASS · 4 PASS(scoped) · 3 PARTIAL · 0 FAIL.**


## Stage 7 outcome — batch 7w (2026-07-24)

**Design system + canonical keybindings + emergency chrome (`86ajp0b3d`) — the first Figma-driven batch.** Canonical tokens implemented as a single Rust source of truth (`selahcue-present::tokens`): UX-CANONICAL §4 fills exact (green `#0f7b6c` / red `#a3283a` / amber `#9a5b00`) + on-dark inks read live from the **Figma design-system variables** (`accent/preview|live|warn`, surfaces, `accent/brand`), reconciled as fill/ink roles and **WCAG-AA-audited in tests on both stacks** (Rust + Dart contrast math, every rendered pairing ≥4.5:1). Canonical keymap (`selahcue_app::keymap`, contract-tested, bounded state): Space/→ next, ← prev, Enter Go Live, **double-Esc Clear-all** (Esc no longer quits), B blackout, Backspace clear-layer; OS auto-repeat filtered; any intervening key — including host pairing keys and the emergency chords — disarms the double-tap. Operator shell: retokenized (preview was wrongly amber), JS keymap mirror, **always-on `#emergency` footer** per the Figma console design (BLACKOUT `B` · CLEAR ALL `Esc Esc` · "Always reachable · pierces any dialog"), chords matched by **physical key code**, reduced-motion honoured; mobile: canonical tokens + brand accent + reduced-motion. Cross-surface **pinning tests** hold webview/Flutter/stage display to the same values. **Review** (run `wf_f4a4b8dc-747`, 40 agents, 5 lenses): 33 confirmed → **14 unique defects (A–N) → all fixed** — incl. a dead emergency chord, auto-repeat defeating the double-Esc gate, focused-button hijack, and stale blackout state while editing (`CODE-REVIEW-batch7w.md`). **Verified: workspace 232 + Flutter 16, clippy/analyze clean.** Also this batch: the user's 4 repo-root TODO notes audited against the PRD/backlog — **all 4 already captured** (FR-014/NFR-019 · FR-040/151/036/037 + R2 FR-039/047 · FR-038 R2 + FR-010 MVP · FR-059) — no new tickets needed; TODO file removed. **Foundation: 23 batches (7a–7w) + 3 refines.**

## Stage 7 outcome — batch 7x (2026-07-24)

**Operator console build-out per Figma (`86ajpgz4t`, story created this batch).** The operator webview is now the Figma console (node 4:2) on the 7w design system: top bar (plan name · red **LIVE** chip when on air · clock), service-plan panel (Figma item styling, NEXT/LIVE outline rows, add row), center **PREVIEW · STAGED** (green header) and **● LIVE · ON AIR** (red header) panels showing the staged/on-air item with **GO LIVE ⏎** between, right **SERVICE TIMER** panel (44px state-coloured readout, TIME UP chip, 5:00/10:00 presets), blackout overlay on the live panel ("BLACKOUT — OUTPUT DARK"), emergency footer + canonical keys unchanged. **Review** (run `wf_f9323e54-033`, 11 agents, 3 lenses incl. a dedicated 7w-regression walk): 8 confirmed → **4 unique → all fixed** — headline: the on-air panels sat below the editor guard, so an emergency clear while renaming left the LIVE panel showing stale truth (moved into the every-view chrome sync); plus aria-labels, aria-live announcements, responsive grid floor. All 14 batch-7w fixes verified preserved. **Verified: workspace 232 + Flutter 16, clippy/analyze clean.** Scoped out (tracked): outputs panel (86ajpew0c), scripture tab (86ajpew05), transcript (R3), +1:00/Pause (86ajp07nr), slide body text (editor stories), mobile console. **Foundation: 24 batches (7a–7x) + 3 refines.**

## Stage 7 outcome — batch 7y (2026-07-24)

**Bundled scripture verse text end to end (`86ajpew05`) — demo step 6 closes its core gap.** New `selahcue-scripture` crate: the **World English Bible** (public domain, 31,098 verses, ebible.org export) bundled as a 1.27MB gzipped TSV keyed by the canonical 1–66 book numbers, decoded once into a bounded in-memory index; verse/passage lookup + case-insensitive keyword search (<500ms perf-tested, warmed). `StageScripture` (and both recovery paths) now compose **actual verse text** — wrapped at 42 cols, capped at the compositor's real capacity (title + 6 body lines, capacity itself pinned by test; truncation always shows "…"). `ScriptureSearch` falls back to keyword hits over verse text. Clients: operator console search-as-you-type (debounced, generation-guarded against stale-hit staging) + panels show staged/live scripture; mobile stages scripture and displays PREVIEW/LIVE status. **Schema v4**: `live_free_text` separates the 7v removed-item free slide from real scripture — recovery restores free slides **verbatim** (byte-identical, regression-tested) instead of recomposing verse text for a title that happens to parse. New serialize-side wire fixture pinned identically in Rust and Dart. **Acceptance proven over the wire:** `wire_scripture_search_stage_golive_shows_verse_text` — keyword search → stage → Go Live on the real TLS link; the host's audience output carries the WEB text of Romans 8:28. **Review** (run `wf_516d2959-967`, 15 agents incl. an audit of the decompressed asset): 10 confirmed → **7 unique (A–G) → all fixed**. **Verified: workspace 244 + Flutter 18, clippy/analyze clean.** Owner QA items this batch → tickets: 86ajphtyh (plan-row layout), 86ajphu2h (operator keyboard focus, high), 86ajphu98 (timer manual entry + live adjust). Remaining story stretch: ASV/BSB/BBE bundles, pagination, mobile search. **Foundation: 25 batches (7a–7y) + 3 refines.**

## Stage 7 outcome — batch 7z (2026-07-24)

**Operator console QA polish (owner-QA bugs `86ajphu2h` + `86ajphtyh`).** Keyboard focus: WKWebView only delivers key events once the page has a focused element — before the first click nothing was focused, so every canonical key was dead on the operator window. Fixed with focus management (focusable body + re-grab on load/window-focus/focusout) and a capture-phase key listener; **needs the owner's on-device re-QA** (headless CI cannot exercise macOS first-responder behaviour). Plan rows: rebuilt to the Figma two-line style (ellipsis title over muted kind, edit tools revealed on hover/keyboard focus) — no more wrapped titles or crowded rows at the 300px column. **Review** (run `wf_b28ed65b-931`): both lenses converged on 1 unique defect — the reveal's `display:none` removed the tools from the tab order (keyboard users lost rename/reorder/delete; an armed delete-confirm could go invisible) — fixed with an opacity reveal that keeps buttons tabbable + a pinned-open confirm window. **Verified: operator check clean, JS syntax-checked, pin tests green (244 desktop + 18 Flutter unchanged).** **Foundation: 26 batches (7a–7z) + 3 refines.**

## Stage 7 outcome — batch 7aa (2026-07-24)

**Display enumeration/assignment + identify + OUTPUTS panel (`86ajpew0c`).** Physical displays enumerate with **collision-free, position-stable keys** (identical-projector venues assignable — the review verified against winit's platform sources that macOS names same-model monitors identically); per-output assignments persist (schema **v5**) and apply as borderless fullscreen at startup and live; **validate-before-persist** ordering means a stale pick can never overwrite a working venue profile or yank the live output to windowed mid-service (the controller also denies keys outside the advertised list, so remotes see the rejection). **Identify** (FR-040): host `I` key or the console's button → each output window blits its number for 5s as a window-level overlay — presentation state is untouched and byte-identical after expiry (tested). The console's right column gains the **OUTPUTS panel** (role, display, resolution, health dot, assignment picker reflecting the persisted key, Identify) with focus-safe rebuilds (an open picker is never yanked; no focus-fall-through to GO LIVE) and async-truthful status (republished on window Moved/Resized, where fullscreen transitions actually land). RBAC: new **ConfigureOutputs**, Operator-only, wire-E2E-tested incl. Producer denial. **Review** (run `wf_5b9ebabf-34e`, 15 agents — rerun after the first attempt hit session limits): 11 confirmed → **7 unique (A–G) → all fixed**. **Verified: workspace 252 + Flutter 19, clippy clean.** On-device 2+-display acceptance is the user's walk (headless CI cannot place windows). Owner requests this session → tickets 86ajpkfcd (scripture chapter browser, high) + 86ajpkfg7 (console layout rebalance). **Foundation: 27 batches (7a–7aa) + 3 refines.**

## Stage 7 outcome — batch 7ab (2026-07-24)

**Scripture chapter browser + KJV default + console rebalance (`86ajpkfcd` + `86ajpkfg7`, both owner requests).** The **KJV ships bundled** (31,102 verses, ebible export, supplied-word brackets + pilcrows stripped, review-audited verbatim) and is the **product default** (owner decision); WEB stays selectable; per-translation APIs with both indexes bounded. The console's center column is now the **Pewbeam-style SCRIPTURES panel**: translation picker (KJV), search box (reference or keywords — translation-aware end to end via a new optional wire field), the full chapter as a numbered verse list, ‹ › chapter paging across book boundaries, **↑/↓ move AND stage the highlighted verse** (held arrows traverse; staging trails 120ms), and the canonical **Enter sends it live** — the owner's arrow-to-live flow with preview→live safety intact. Typed ranges ("John 3:16-18") stage the whole passage. Preview/Live shrink to **16:9 thumbnails** stacked right with the timer + OUTPUTS panel (the owner's "panels too big" feedback). **Review** (run `wf_c5226f85-e91`, 13 agents): 9 confirmed → **6 unique (A–F) → all fixed** — headline: Enter-on-keywords silent dead-end; range collapse; translation switches carrying a raw INDEX across divergent KJV/WEB numbering (six chapters differ — verified in the assets). New owner story: `86ajpqfyj` (more translations: PD bundles + a licensing spike for NIV/NLT/AMPC/NKJV/TPT/MSG — copyrighted, cannot be bundled). **Verified: workspace 253 + Flutter 19, clippy/analyze clean.** Known gap (documented): the chosen translation is not persisted — recovery recomposes in KJV. **Foundation: 28 batches (7a–7ab) + 3 refines.**

## Stage 7 outcome — batch 7ac (2026-07-24)

**Timer manual entry + live add/subtract (`86ajphu98`, owner request).** `Timer::adjust` mutates a running countdown's target monotonic-safely — elapsed untouched, clamped to the **persistable 0..=u32::MAX-seconds domain** (the review proved an unclamped wire-legal delta would make the recovery snapshot wrap modulo 2³², restoring a different timer than the pre-crash session); the legacy add/subtract helpers now delegate so every path shares the clamp. Wire `AdjustTimer{delta_secs}` (fixture-pinned, Timer permission, idle → denied). Console timer panel: minutes entry (sanitized, 1..=999) + **+1:00/−1:00** enabled only while a timer runs; mobile gets the same plus silent state-race denials (a ±tap in the poll gap after a remote stop no longer raises a misleading banner); the stand-alone demo shell now ticks so timers work in Local mode. **Acceptance test-proven:** 5:00 → +1:00 at 1:20 → 4:40 remaining, persisted through crash/restore; −overshoot lands in TIME UP. **Review** (run `wf_e358648f-a84`): 6 confirmed → **4 unique (A–D) → all fixed**. **Verified: workspace 256 + Flutter 20, clippy/analyze clean.** Owner request this session → ticket 86ajpv0ub (search-result snippets with match highlighting, Pewbeam-style). **Foundation: 29 batches (7a–7ac) + 3 refines.**

## Stage 7 outcome — batch 7ad (2026-07-24)

**Search-hit highlighting (`86ajpv0ub`) + crash-loop breaker & storage guard (`86ajp09td` remainder).** Search results now show **why they matched** (owner request, Pewbeam-style): each hit carries its verse text on the wire (skip-if-empty — fixtures byte-identical, pre-7ad hosts fall back to bare references), rendered as reference + translation chip + a one-line snippet **windowed onto the first match** with every query word highlighted (bold + amber fill, AA, escape-safe DOM); ↑/↓ select hits, Enter opens. **FR-169 breaker:** 3 unstable launches in 60s → the app starts clean **with checkpointing disabled for that run** — the review caught that the naïve version would have UPSERTed demo state over the very session it promised to preserve; now the preserved session is untouched and a stable relaunch resumes it; stability (10s uptime or clean exit) forgives the journal (bounded, skew-safe, unit-tested). **NFR-023 storage guard:** <100MB warns, <10MB halts checkpoint writes — enforced from startup (not just the first 60s check), honoured by the exit save, and an unreadable free-space reading while halted keeps reporting instead of going silent. **Review** (run `wf_a3c5448f-f47`, 12 agents): 10 confirmed → **8 unique (A–H) → all fixed**. **Verified: workspace 259 + Flutter 20, clippy/analyze clean.** Honest deltas for owner disposition on 86ajp09td: the GUI Resume-vs-Start-clean dialog (delivered as automatic-clean + relaunch-to-resume) and per-item disable attribution. **Foundation: 30 batches (7a–7ad) + 3 refines.**

## Stage 7 outcome — batch 7ae (2026-07-24)

**PD translation bundles (`86ajpqfyj` Track 1) — five translations shipped.** ASV (31,086), **WEB British Edition** (31,098), and Darby (31,099) join KJV (default) and WEB — all selectable in the chapter browser, all decoding lazily and exactly once (untouched translations cost only compressed bytes; the no-leak test loops all five). **The review's licensing audit earned its keep:** BBE — originally converted and bundled — turned out to be **US-only public domain** (Cambridge UP; the translator died in 1968, so life+70 jurisdictions incl. the UK, EU, and Nigeria plausibly hold copyright until 2038); it was **dropped before ship**, substituted with the worldwide-safe WEBBE, and the licensing register corrected (the BBE row now carries the owner decision for any US-only distribution). Also caught: BBE's literal `***` placeholder verses (1 Sam 13:1 was *only* asterisks) and Darby's 19 dangling footnote asterisks in Psalm 119 — the pipeline now strips the class and a **full-corpus residue test scans every verse of every translation** (~155k). Version safety: the **host advertises its translation list on the wire**, so a newer console never offers codes an older host would silently deny. **Verified: workspace 260 + Flutter 20, clippy/analyze clean.** YLT (no usable export), BBE (owner decision), and the licensed set (NIV/NLT/… licensing spike) remain on 86ajpqfyj. **Foundation: 31 batches (7a–7ae) + 3 refines.**

## Stage 7 outcome — batch 7af (2026-07-24)

**Double-click a verse → straight to live (`86ajpwcxc`, owner request) + the consolidated QA walk-through.** In the chapter browser, a single click stages (preview→live safety) and a **double-click sends the verse to the audience in one gesture** — with every step VERIFIED: the review (run `wf_ac212b67-bca`) confirmed the naïve flow could commit *whatever was previously in Preview* when a host denied the stage (e.g. an older host rejecting a translation code — "Amazing Grace" going live while the status claimed a verse), could claim "→ LIVE" unconditionally even on transport failures, and could interleave a debounced arrow-stage between the stage and the commit on slow links. The shipped flow captures the reference before any await, checks the returned view's `staged_scripture` before committing, blocks debounced stages while in flight, and only announces LIVE when the view confirms **this** verse is on air (honest failure messages otherwise). Also this batch: **`docs/delivery/QA-WALKTHROUGH.md`** — a 13-step, ~15-minute ordered script covering all 13 QA items for the owner's on-device pass. **Verified: pin tests + operator check + JS syntax; suites unchanged (260 desktop + 20 Flutter).** **Foundation: 32 batches (7a–7af) + 3 refines.**

## Stage 7 outcome — batches 7ag + 7ah (2026-07-24)

**7ag — reference shorthand (`86ajpwkte`, owner request) + a live-reported fix.** "gen 1 1" / "1 cor 13 4" / "gen 1 1-3" resolve as book chapter:verse everywhere (parser fallback fires only when the punctuation form fails — every existing spelling untouched; 261→262 tests). The owner caught "2 cor 4 5" landing on verse 1 while testing live: the parser was right but the browser's cursor-landing understood only colon syntax — `get_chapter` now returns the PARSED verse selection and the browser lands/stages from it uniformly (shorthand ranges stage whole passages too). Review (run `wf_d5634e66-b97`): 1 confirmed (low — the absent-verse guard's status was clobbered by the fallback search) → fixed forward in 7ah. **Ticket-state audit (owner request): the owner closed 7 QA items** (keyboard focus, plan rows, timer ±, design system, console, layout, plan persistence); 7 remain in QA; 86ajpqfyj corrected to in-progress.

**7ah — SBOM + license policy + GPU device-loss assertion (`86ajpew0j`).** `deny.toml` on BOTH cargo trees (permissive-only — no copyleft; unknown sources denied; own crates private via `publish = false`); a new CI **supply chain** job runs the policy and uploads CycloneDX SBOMs per build; NFR-024 gets a real assertion — `Compositor` gained a device-lost callback seam + fault injection, and the new test proves the loss signal fires, nothing panics, and a recreated compositor renders pixel-identically (executes in the 3-OS matrix; lavapipe guarantees Ubuntu). Verification = the hosted run itself + local policy runs (review waived as redundant for config, 7s precedent). **262 workspace + 20 Flutter tests, clippy clean.** Flutter SBOM noted as story remainder. **Foundation: 34 batches (7a–7ah) + 3 refines.**

## Stage 7 outcome — batch 7ai (2026-07-24)

**SQLCipher key acquisition (`86ajp5vp6`) — the last high-priority foundation story.** The `encryption` build now acquires its key for real: **OS secret store** (256-bit random, generated and stored on first run — macOS Keychain / Windows Credential Manager / Linux Secret Service) with an **Argon2id passphrase** path (`SELAHCUE_PASSPHRASE`, OWASP parameters, per-install `key.salt` persisted beside the store; backups must include it — the app warns loudly if it's lost). The security review (empirical — it built SQLCipher and reproduced every failure mode) found the naïve open path could be **wedged forever by one first-run keychain hiccup** (a plaintext store minted beside real data, then every healthy run failing with corruption-looking errors and a FALSE "opening UNENCRYPTED" message inviting the operator to delete recoverable data). The shipped open path is a **header-discriminated state machine**: plaintext stores stay plain (honest migration-pending note), encrypted stores with a bad/missing key **hard-stop persistence with the true cause and an explicit "do NOT delete" — file byte-untouched (test-asserted)**, and unencrypted creation exists only for a nonexistent file. Intermediate key material zeroized. CI/make encryption lanes extended to the desktop crate. **11 encryption-lane tests; 262 workspace + 20 Flutter; clippy + deny green.** Recorded remainder: a rekey/plaintext-migration tool. **Foundation: 35 batches (7a–7ai) + 3 refines.**

## Stage 7 outcome — batch 7aj (2026-07-25)

**mDNS host discovery (`86ajp0b0t` remainder).** The desktop advertises `_selahcue._tcp` (auto-detected interface addresses, per-run unique instance) while the control server runs; the phone's pairing screen gains a **Nearby hosts** list that browses on demand — no typing an address. **The security review earned the batch:** it proved that supplying the cert pin over mDNS decoupled the two secrets the QR model binds atomically, so a **rogue host advertising its own pin could phish the real host's single-use pairing code and pivot to Producer control** (go-live/blackout/timers). Closed with a **short-authentication-string gate**: the host prints/advertises a pin **fingerprint** (`AB12-CD34-EF56`), the phone shows the discovered fingerprint, and the code is disclosed **only after the operator confirms the two match** — a rogue's fingerprint won't. Also fixed: iOS `NSLocalNetworkUsageDescription`/`NSBonjourServices` (discovery was silently dead on iOS 14+), the Android multicast permission, multi-NIC A-record advertising, and instance-name collisions. Discovery stays best-effort (an mDNS-hostile network just shows an empty list; QR/manual paths unchanged and safe). **Verified: workspace 263 + Flutter 23, clippy/deny/analyze clean.** Recorded remainders on the story: the Android MulticastLock (native runtime acquisition) and on-device phone QA (real camera scan + real browse on iOS/Android). **Foundation: 36 batches (7a–7aj) + 3 refines.**

## Stage 7 outcome — batch 7ak (2026-07-25)

**Figma UI update (`86ajpx7c0`, owner request) — design work, no repo code.** Built a new **Mobile — Producer** screen in the Figma file (`SYQn5hFY8YVQKm3c6rw0eJ`, node 119:2) reflecting the shipped console's design language on a phone: top bar (plan name · PRODUCER role chip · LIVE chip), labeled 16:9 PREVIEW·STAGED / ●LIVE·ON AIR output cards with GO LIVE between, a SERVICE PLAN list (a reusable `MobilePlanRow` component, instanced with LIVE/PREVIEW badge states), scripture quick-entry showing the `gen 1 1` shorthand, a timer strip (readout + −1:00/+1:00/Stop), and an always-on emergency bar (BLACKOUT / CLEAR ALL, non-colour labels). Every fill is bound to the file's `SelahCue Color` design-system variables (accent/preview #2bb673, accent/live #ef4444, accent/warn, bg/*, border, text/*) — not hardcoded — so it stays linked to the same tokens the code pins to. This is the concrete design the **mobile UI revamp story (`86ajpx7bd`)** builds against.

**Refine (2026-07-25, owner feedback "cramping all the screens is not a great UX"):** invoked the `/ui-ux-designer` skill and redesigned the mobile Producer experience around **progressive disclosure** instead of one crammed screen. New IA: a **bottom tab bar (Live · Plan · Scripture · Timer)** with a **persistent emergency strip (Blackout · Clear All) above it on every screen**, and a compact top bar (plan name · LIVE pill · clock). Four focused phone frames built in Figma (nodes 123:16–123:19), each doing one job — **Live** (preview/live cards + ◀ GO LIVE ▶ transport + next-line hint), **Plan** (read-only list, tap-to-stage, "Go Live from the Live tab"), **Scripture** (translation picker + search + verse list with the cursor verse highlighted + chapter ‹ › nav + tap/double-tap hints), **Timer** (72px readout + presets + minutes + ±1:00/Stop). Shared chrome as reusable components: `MobileTopBar`, `MobileEmergencyStrip`, and a `MobileTabBar` variant set (active per screen). Every fill bound to the `SelahCue Color` variables; canonical colour+label redundancy throughout. Emergency actions never scroll away and never compete with content — the core UX fix.

Task stays in progress: the console-frame refresh (node 4:2 → shipped layout: scriptures center, 16:9 thumbnails, outputs panel) is the continuing half. **Foundation: 37 batches (7a–7ak, 7ak refined) + 3 refines.**

## Stage 7 outcome — batch 7al (2026-07-25)

**Mobile Producer revamp shipped in Flutter (`86ajpx7bd`) — the 7ak design, built.** The one crammed controller screen is replaced by a **progressive-disclosure tabbed shell**: a bottom `NavigationBar` (Live · Plan · Scripture · Timer over an `IndexedStack`) with a **persistent emergency strip (Blackout · Clear All) above the nav on every tab** so the panic controls never scroll away. The connection story is now a real flow — a branded **Splash** reconnects returning users behind a 900ms brand moment and drops first-run/failed users onto a **Connect** screen that leads with an auto-browsing **Nearby hosts** list (Scan QR + paste behind it). Three owner refines landed in the same batch: **single-tap stages to Preview, double-tap sends live** in one gesture on both the plan and scripture tabs (each verifies the stage actually landed before committing — never sends stale Preview); an **About / connection drawer** showing host:port, Producer role, the pin fingerprint and live status, with a **Disconnect** (un-pair); and **offline scripture autocomplete** (a bounded 66-book suggester — "gen" → Genesis, "1 co" → 1 Corinthians — that stops suggesting once a chapter number is typed).

**The adversarial review (13 agents, three lenses each independently verified to refute) earned its keep:** the batch's new command-denial banner was **inert** — `act()` set the error, then its own `refresh()` *and* the 1s poll both did `_error = null` on success, so a denied CLEAR ALL/BLACKOUT flashed sub-frame and the RBAC-limited Producer saw nothing and would hammer the control mid-service. Closed by splitting the transient connection status from a **sticky denial** that survives the poll (cleared only by dismiss or a command that actually goes through) — regression-tested via a new `ControllerSession` interface and a fake session. Also fixed: glyph-only ◀/▶ transport buttons had no accessible name (added `Semantics(button, label)` across the custom controls), a blank AppBar on an unnamed plan, Nearby-hosts never auto-searching, and a splash session-leak window. Three findings correctly refuted. **Verified: `flutter analyze` clean + 29 Flutter tests (+3 controller denial-lifecycle).** Recorded remainder on the story: on-device QA (posted as list-form steps) and mobile verse-list parity (needs a wire path). **Foundation: 38 batches (7a–7al) + 3 refines.**

## Stage 7 outcome — batch 7al refine (verse list + drawer removal) (2026-07-25)

**Owner refine, three asks — the mobile verse list is now the desktop browser on the phone, the drawer is gone, and Figma is in sync.** (1) The About/connection **nav drawer** — a second navigation surface fighting the bottom tabs — was **removed** and relocated to a top-bar ⓘ action that opens a modal sheet (live status + Disconnect), and a wall clock was added. (2) The Scripture tab became a real **chapter verse list**: because the phone carries no scripture bundle, this added a **read-only `GetChapter` wire command** (Rust `Command::GetChapter` + `ServerMessage::Chapter` + `VerseView`, RBAC-gated on `SearchScripture` which the Producer holds, served straight from the existing `chapter_in`/`adjacent_chapter_in` crate API; Dart `cmdGetChapter`/`ChapterResult`/`LiveController.fetchChapter`). **No protocol version bump** — the command is additive at v2, so an older host simply rejects the unknown `cmd` and the client **degrades gracefully** to reference-only staging. The tab now offers a translation picker, `‹ Book Chapter ›` chapter paging, numbered verses, and **single-tap → Preview / double-tap → Live** (the browsed translation threaded through staging; the staged verse shows green, the live verse red). (3) **Figma ↔ implementation synced**: the Figma Scripture frame already specified this verse list (the app was built to it), the drawer was never in the design, and the About/info affordance was added to the shared `MobileTopBar` component — both now read **plan name · ● LIVE · clock · ⓘ About**.

**The review paid for itself again:** its headline was that a verse which is both staged AND live rendered **green (preview) instead of red (live)** — `_VerseRow` tested `staged` before `live`, but the host reports `staged_scripture == live_scripture` after go-live (GoLive copies staged into live and never clears staged), so the verse actually on the audience wore the preview colour and announced "staged". Fixed by ordering **live before staged** (matching `plan_tab`), and locked with a verse-coloring regression widget test. Also fixed: a silent failed-lookup (now a SnackBar + field re-sync, keeping the current chapter), a transient socket blip falsely showing the "old host" fallback on a healthy host (retry once across the reconnect), the "open with staged/live chapter" preload that never fired (the view is null at mount — now listens until it arrives), and a stale reference field after ‹ › paging. One low-severity item accepted as-is: the ~300ms single-tap latency is inherent to the double-tap-to-live gesture (same as the shipped plan tab). **The wire-compat lens found nothing** — the cross-language protocol path is clean, fixtures pinned on both sides. **Verified: 266 Rust tests (+3) + 36 Flutter tests (+ verse-coloring widget test), clippy/analyze clean.** **Foundation: 38 batches (7a–7al) + 4 refines.**

## Stage 7 outcome — batch 7am (emergency-controls closeout + per-layer deferral) (2026-07-25)

**Urgent story `86ajp0awx` (Emergency clear/blackout + per-layer clearing) closed out — verified, hardened, and honestly scoped.** Blackout, clear-all, the canonical keybindings (`B`, `Esc·Esc`, `Backspace`, arrows/Space/Enter) and always-on emergency chrome were already shipped in earlier batches; this batch adds the story's **required verification tests** and resolves the one open item. New: a **webview keybinding + modal-pierce contract test** — a Rust test that reads the operator `index.html` and pins the JS keymap to the canonical Rust map, AND asserts (structurally) that the global emergency chords (`Ctrl/Cmd+Shift+B` / `+.`) are handled **before** the input-focus early-return so they fire even while a text field or dialog owns focus, capture-phase; and a **blackout/clear offline+<200ms** test proving the operator's controls act on the in-process presenter with no LAN round-trip, taking effect well inside the 200ms budget.

**The one remaining piece — per-layer clearing — was deferred, not faked.** Tracing the model showed the live output is a **single `Slide`** with no independently-addressable persistent layers, so "clear the current layer" is identical to "clear all" today; real per-layer clearing needs coexisting overlay layers (song base + scripture overlay + lower-third), which is the **R2 Media & Output Expansion** territory (no overlay producers exist yet). With owner approval it was **split into `86ajpy59e`** under the R2 epic, and the `ClearLayer`-aliases-to-Clear-all comments (`keymap.rs`, `main.rs`, `index.html`) now point there. The independent review's closeout-soundness lens confirmed the deferral is sound; its two low-severity findings (both in the new tests — an unpinned Space→Next binding and a comment-coupled capture-phase check) were fixed. **Verified: 268 workspace tests (+2), clippy clean.** Handled per the `/build` team skills (`backend-engineer` for the wire/architecture assessment, `qa-engineer` for the test strategy). **Foundation: 39 batches (7a–7am) + 4 refines.**

## Stage 7 outcome — batch 7an (cross-OS GUI-launch smoke + per-OS NFR) (2026-07-25)

**Story `86ajpevzp` — the app is now proven to LAUNCH (windows created, first frame presented) in CI, not just build+test; executed end-to-end through the `/goal` engine against a validated Goal Contract.** A GUI-launch **smoke mode** (`--smoke` / `SELAHCUE_SMOKE`) presents the main output window's first frame, prints the time-to-first-frame, and exits 0; a 30s in-app watchdog plus a CI `timeout-minutes` backstop fail loudly instead of hanging on a wedged launch. A new **`launch-smoke` CI matrix job** runs it on **Linux headless under Xvfb + lavapipe** and **macOS** — both green (windows created + frame presented + exit 0). **Windows** is a documented runner limitation: the app launches and presents there (recorded 522ms) but GitHub's headless windows-latest runner intermittently never presents (a DXGI/desktop-session flake, not an app defect), so it's excluded from the required matrix while the rust + operator lanes keep covering Windows build+test.

**The batch also fixed a real cross-OS bug and hardened the NFR gate:** `make nfr` was broken on Linux (`mktemp -t <prefix>` is a BSD-ism GNU rejects — the NFR script only ever ran on macOS); it now runs on Linux and records **cold start 0.058s / idle 203MB** (both within the 3s/300MB budgets, now **gating** in CI). **The independent review earned its keep on verification integrity:** it caught that the Goal Contract's C-004 overstated the ≤3s cold-start by pointing at the 5.2s software-lavapipe first-frame instead of the `make nfr` control-server proxy — reframed honestly (the smoke first-frame is informational; the ≤3s bar is the proxy: Linux 0.058s / macOS 1.13s). Also fixed from the review: the watchdog couldn't catch a hang *inside* window/adapter creation (added the CI timeout backstop), the NFR budgets were `continue-on-error` (now gate), and `--smoke` no longer persists the session store. **11 raised → 7 confirmed → all fixed; 4 refuted.** Handled per the `/build` team skills (`devops-engineer` lead, `/goal` execution engine) with the Goal Contract validator passing `--require-complete`. **Foundation: 40 batches (7a–7an) + 4 refines.**

## Stage 7 outcome — batch 7ao (Android MulticastLock for mDNS discovery) (2026-07-25)

**Story `86ajp0b0t`'s recorded Android remainder — Nearby-hosts discovery now actually works on Android; executed through the `/goal` engine.** The Flutter controller discovers hosts with pure-Dart `multicast_dns`, but Android's Wi-Fi stack **silently filters inbound multicast unless an app holds a `WifiManager.MulticastLock`** — so the browse found nothing on Android even though the `CHANGE_WIFI_MULTICAST_STATE` permission had landed in 7aj (the runtime lock was the missing half). This batch wires it end-to-end: a Dart `MulticastLock` abstraction + a best-effort `PlatformMulticastLock` (a `selahcue/multicast` MethodChannel, Android-only, a no-op elsewhere), `DiscoveryController.refresh()` acquiring the lock before the browse and **always releasing** it (the mDNS browse extracted behind an injectable seam), and a Kotlin `MainActivity` channel handler that acquires/releases a non-ref-counted `MulticastLock` (`isHeld`-guarded, released on `onDestroy` — no leak). The Kotlin is compile-verified in CI by a new **`flutter build apk --debug`** step.

**The review's one confirmed find was a real robustness hole:** `refresh()` had no `catch`, so a throwing browse would propagate and leave `_searching=true` forever — a **permanent discovery lockout** (every later refresh a silent no-op, the spinner pinned on), masked in production only because `_mdnsBrowse` swallows internally but undefended (the client construction + `stop()` can throw). Fixed by making `refresh()` best-effort (catches — never throws, always resets), with the test rewritten to prove no-wedge + recovery. **3 raised → 1 confirmed → fixed; 2 refuted.** **Verified: `flutter analyze` clean; 39 Flutter tests (+3 lock-lifecycle); CI green including the APK build.** On-device discovery (a real Android phone finding a host over mDNS) is explicit owner QA — not automatable in CI. Handled per the `/build` `mobile-engineer` skill + the `/goal` engine (validator `--require-complete` PASS). **Foundation: 41 batches (7a–7ao) + 4 refines.**

## Stage 7 outcome — batch 7ap (Figma operator-console refresh) (2026-07-25)

**Story `86ajpx7c0`'s remaining half — the design source-of-truth now matches the shipped desktop console; executed through the `/goal` engine.** The Figma console frame (`4:2`) was the pre-shipping concept (Preview/Live 2-up in the centre, a live-transcript band and a scripture auto-detection/approve band — both R3/R4, not in the MVP — and a "Cloud OFF" chip). The shipped operator console has evolved to a simpler 3-column MVP layout, so this batch built a faithful **new** frame `150:124` "Operator Console — shipped (86ajpx7c0)": header (plan name · ● LIVE · clock) → left **Service plan** (PREVIEW/LIVE outline rows + add-row) → **centre Scriptures** (translation picker · search · ‹ chapter › nav · numbered verse list with the cursor verse highlighted green, amber numbers) → right column (**PREVIEW·STAGED** 16:9 → GO LIVE → **●LIVE·ON AIR** 16:9 → Service timer → Outputs → Identify) → emergency footer (Blackout · Clear All). Every fill is bound to the "SelahCue Color" design-system variables (extended, not forked), and the R3/R4 concept bands were deliberately dropped; the concept frame `4:2` is preserved for history.

**Independent design review (a reviewer agent comparing the frame to the shipped `index.html`) found it structurally faithful — no high-severity issues, post-MVP bands correctly absent — with 3 medium divergences, all fixed:** the BLACKOUT off-state was made a dark panel button (it had rendered light; it's a canonical emergency control that's only red when active), the timer controls were laid out across the shipped 3 rows (presets · minutes+Start · −1:00/+1:00/Stop), and the Outputs panel was given the real role labels (Main output / Stage display) + a per-row Assign picker + a full-width Identify button. Low-severity stylization latitude documented (the shipped code remains authoritative for exact content/interaction states). No repository code changed — the artifact is the Figma frame. Handled per the `/build` `ui-ux-designer` skill + the `/goal` engine (validator `--require-complete` PASS). Both halves of `86ajpx7c0` (mobile + console) are now done. **Foundation: 42 batches (7a–7ap) + 4 refines.**

## Stage 7 outcome — batch 7ap refine (Operator UX: transcript + detections; queue → service plan) (2026-07-25)

**Owner refine — a genuinely better Operator experience, and a good design-critique adopted.** The owner wanted the Operator console to include a **Live transcript** + **Recent detections** (auto-detected scriptures with confidence · Add-to-plan · Preview) like Pewbeam — my 7ap frame had matched the shipped MVP, which deliberately omits them (they're R3 transcription / R4 scripture-intelligence, post-MVP). Since the ui-ux-designer designs the target experience ahead of implementation, this is the **target** Operator design. Built a new 4-zone frame `161:124` on SelahCue's own tokens (green preview / red live / amber warn — not Pewbeam's orange), deliberately **uncramped** per the earlier "don't cram" feedback: **Service Plan** (the running order) · **Listen** (Live transcript with interim/final + a "listening…" cue + level meter + Pause/REC, and Recent detections with HIGH/MED/LOW confidence + Add-to-plan/Preview) · **Program** (Preview/Live 16:9 monitors + GO LIVE + a "Coming next" plan strip) · **Reference** (scripture browser + timer + outputs) + the always-on emergency footer.

**The owner's design instinct was right and adopted:** they asked why a Queue is needed when there's a Preview — it isn't. In SelahCue the **Service Plan** is the ordered running list and **Preview** is the single on-deck item (preview→live safety); a detection just goes to Preview or into the Plan. The Queue (a Pewbeam-ism) was **removed** and the **Service Plan restored** (it had been dropped when the Listen zone was added). An independent design-reviewer agent judged the design "faithfully delivers the intended better Operator UX" (on-brand, confidence mapping correct, uncramped); its 2 medium + 3 low findings were fixed (transcript clarity cue + legend, the detection action renamed **Preview** for a consistent live model, HIGH/MED/LOW labels for colour-blind users, the duplicate header BLACKOUT removed, the amber Pause neutralized). No repository code changed. The owner then directed a smoother, more OBS-native layout, delivered as the final frame **`165:124` "Operator Console — OBS studio (target)"**: **transcript + recent detections moved to the right** (with the **timer in the bottom-right corner**), **PROGRAM as OBS studio-mode** (Preview | Live horizontal + the GO LIVE transition below), and **Scriptures directly beneath PROGRAM**. The owner then directly edited `165:124` — moving the **transcript to the bottom-left** (under the Service Plan), the **timer to the top-right**, detections below it, outputs bottom-right, and making Scriptures taller — and asked the designer to note + fine-tune the changes; done: the transcript was reflowed for its narrower column (it had clipped), Outputs rebuilt at the right width (it had overflowed the frame edge), the right column aligned, and the timer tightened. Final zones: LEFT Service Plan + Live transcript · CENTER OBS Preview|Live + GO LIVE + Scriptures · RIGHT Service timer + Recent detections + Outputs · emergency footer — all on the SelahCue tokens. **Recommend linking `165:124` from the R3/R4 epics** when they're planned. Handled per the `/build` `ui-ux-designer` skill + the `/goal` engine (validator `--require-complete` PASS). **Foundation: 42 batches (7a–7ap) + 5 refines.**

## Stage 7 outcome — batch 7aq (licensed-translations spike + provider ADR) (2026-07-25)

**Story `86ajpqfyj` Track 2 — the owner's requested copyrighted translations (NIV, NLT, AMPC, NKJV, TPT, MSG) are now decision-ready; executed through the `/goal` engine with an ultracode research fan-out + adversarial source verification.** A 4-lens web research pass (TPT/AMPC rights · API.Bible commercial terms · direct publisher routes · 7 comparable products; primary permissions pages wherever reachable, Cloudflare-blocked ones via dated Wayback snapshots) produced `docs/research/LICENSED-TRANSLATIONS.md`: none of the six is ever bundlable (every gratis policy caps at 500–1,000 verses, Lockman's including an electronic-storage cap); **API.Bible is the only lawful aggregator** ($29/mo Pro + $10–250/mo per translation tiered by user count, FUMS reporting, a binding 30-day cache-refresh rule — online-mostly by construction); the **industry pattern** (ProPresenter $15/computer, EasyWorship $29–39, MediaShout site-licence bundling) is per-translation purchases on direct publisher licences; **TPT has no aggregator route** and carries documented stability/reputational risk (Bible Gateway removal 2022, text mutating until ~2029 — the owner is asked to re-confirm wanting it). **ADR-0017** (Proposed) defines the pluggable `TranslationProvider` seam: licensed providers become adapters behind the 7ae host-advertised-translations surface — zero client changes, licence-honouring bounded caching, a FUMS-style displayed-hook, attribution on the output, honest degradation, keychain credentials.

**The adversarial verification pass earned its keep:** 6 fact-checkers re-fetched the primary sources — **4 CONFIRMED + 2 CORRECTED (0 unverifiable)** — and the headline correction is decisive: **NIV is verifiably EXCLUDED from commercial use on API.Bible** (upgraded from an untraced snippet to OBSERVED/High), so **NIV's only commercial route is the direct Biblica Standard Publishing License** (case-by-case royalties; Biblica prefers finished products → post-MVP application). Also corrected: the supposed 14-vs-30-day cache conflict (30 is binding, 14 is a recommendation) and ProPresenter's free-list details. The register gained TPT + AMPC rows and refined NIV/AMP/MSG rows. ClickUp: **DECISION `86ajpzb09`** (owner + counsel — route/budget/TPT re-confirm) and **STORY `86ajpzb0c`** (provider implementation per ADR-0017, waiting on the decision). Handled per the `/build` `product-researcher` skill + the `/goal` engine (validator `--require-complete` PASS).

**Refine (owner): "can users download the other versions for offline access?"** Verified answer: **offline yes — through download-on-purchase under SelahCue's own licence; user-sourced downloads no.** No lawful public source exists for the six (retail digital copies are app-locked: e-Sword/Olive Tree/Logos prohibit export), but **EasyWorship's purchased Bibles verifiably install locally** (with an offline-registration USB flow for never-online machines) and **API.Bible's terms explicitly permit offline storage conditionally** (DRM, 100-verse print cap, device limits, 30-day refresh checks, 72h termination deletion — T&C §10–12, quoted verbatim). Dossier §4b added; **ADR-0017 upgraded with an entitlement-gated encrypted offline store** (keychain-keyed, print-cap hook, device binding, refresh + revocation); the DECISION and implementation tickets updated — a user activates a translation, the app downloads it into the protected store, and **Sunday morning works fully offline**. **Foundation: 43 batches (7a–7aq) + 6 refines.**

## Stage 7 outcome — batch 7ar (foundation-demo milestone re-score) (2026-07-25)

**Milestone `86ajp0bpn` re-scored against current evidence — the foundation demo now has ZERO partial and ZERO failing steps; executed through the `/goal` engine with 9 fresh-context auditors (no self-scoring).** The auditors (Workflow `wf_4e58027d-bb3`, 323 tool calls) re-ran the cited test suites and pulled live CI runner logs rather than trusting documents. **V2 score: 2 PASS · 7 PASS (scoped) · 0 PARTIAL · 0 FAIL** (7t-era: 2·5·2·0). Upgrades on concrete evidence: **step 1** (the 7an launch-smoke CI proves real GUI launch to a presented frame on Linux+macOS continuously, plus a runner-log-confirmed one-time Windows launch at 522 ms; NFR budgets now gate), **step 5** (the owner personally QA'd and closed `86ajphu98`, whose scripted steps include the stage countdown + TIME UP), **step 8** (mDNS + SAS + MulticastLock + the mobile revamp, 12→39 Flutter tests). The audit also **corrected the old review against itself**: the plan "library" is data-layer + tests only (no shipped client exposes search/duplicate — kept as a story delta), and step 9 was honestly refined PASS→PASS(scoped) (the live kill-walk is 7u-era; no process-level kill test — both on the new hardening follow-up `86ajpzbxf` with the cross-language pairing E2E and one doc relocation).

**Everything left is either owner-hardware QA (already posted as list-form steps on the QA tickets: camera QR scan + on-device mDNS, the 2+-display walk, the cable-out emergency demo, visual re-confirmations), a documented toolchain limit (Windows encryption tests / NFR / launch-smoke continuity), or a tracked descope.** Disposition recommendation at the gate: **conditionally met** — the owner runs the posted on-device/multi-monitor QA; if green, close the milestone and Stage 7, and open the Stage-8 planning gate. **Foundation: 44 batches (7a–7ar) + 6 refines.**

## Stage 8 planning prep — batch 7as (2026-07-25)

**The Stage-8 (Core presentation) plan is gate-ready** (`delivery-manager` via the `/goal` engine; docs + a Build Control comment only — **no stories created before approval**). `STAGE8-core-presentation.md` (DRAFT) proposes 10 stories mapped 1:1 to the existing epics' scope text — songs (multi-slide items), text shaping (ADR-0014), themes/templates, the console editor, CCLI footers, the sandboxed decode path + image slides + missing-media placeholder (ADR-0016), the pre-service check, video/audio (spike S2 first), timer remainders, and per-layer clearing becoming real once layers exist — with the critical path (8a songs → 8b shaping → 8c themes → 8d editor), a parallel media track, named conflict surfaces, and the stage's biggest risk (video/audio GPU interop) spike-gated. Gate choice presented: **songs-first (recommended)** vs media-first. Stage 8 opens only on the owner's `continue`; Stage-7 closure (on-device QA) runs in parallel.

## Stage 8 outcome — batch 8a (Songs: multi-slide items, S8-1) (2026-07-25)

**Stage 8's first implementation batch — plan items can now be real multi-slide songs; executed through the backend-engineer skill + the `/goal` engine.** A `PlanItem` gained stanza content (a stanza = the lines of one slide); `Next`/`Previous` advance slide-by-slide **within** a song before crossing plan items; `GoLive` commits the current slide; the confidence monitor's "next" follows the song's next stanza; a PD-hymn plain-text importer (blank-line-separated stanzas) creates songs; and mid-song position survives a force-kill. Persistence is a **v6** additive migration (NULL `content` = title-only, every existing row unchanged). The wire gained `slide_count`/`slide_index` on `PlanItemView` + `AddItem.content`, all additive (`serde(default, skip_serializing_if)`) so the pinned cross-language fixtures stay byte-identical (VERSION unchanged). Clients: the operator webview shows "song · 2/6" + a lyrics box on the add-row; the Flutter controller parses the fields + a slide badge.

**The adversarial review (`wf_bee2e975-576`, 4 verified lenses) caught one real regression I'd introduced:** removing a LIVE song then crashing recovered only the title — the free-slide capture stored `slide.title` (lossless pre-8a when live slides were title-only, but songs now carry a body). Fixed by capturing + persisting the body (`live_free_body`, append-only migration **v7**) and restoring `Slide::new(title, body)` verbatim, with a regression test. The other 3 lenses (navigation, wire compat, migration safety) were clean. **+22 Rust / +2 Dart tests; full workspace + `--features server` E2E + Flutter green; fmt+clippy clean; CI run 30156606042 green (11 jobs, 3 OSes).** Story `86ajpzha3` → QA; contract `--require-complete` PASS. **Stage 8: 1 batch (8a); next 8b Text shaping (`86ajpzha7`).**

## Stage 8 outcome — Operator console OBS layout (Figma 165:124, `86ajpztgr`) (2026-07-25)

**Owner-directed: implement the approved OBS-studio operator-console design (Figma 165:124) in the Tauri webview.** The console `dist/index.html` was restructured into the 3-zone OBS layout — **LEFT** Service Plan + Live Transcript · **CENTER** OBS Preview | Live side-by-side + GO LIVE + Scriptures · **RIGHT** Service Timer + Recent Detections + Outputs · always-on emergency footer — as a pure re-parenting of existing DOM subtrees into three `.zone` wrappers plus two new panels. **The entire `<script>` is byte-unchanged and all 41 wired element ids are preserved** (only the unreferenced `#rightcol` wrapper removed; dead CSS cleaned up), so every control (plan editing, scripture browser, Preview/Live, GO LIVE, timer, outputs, Identify, the emergency keymap + modal-pierce chords) keeps working. The **Live Transcript** + **Recent Detections** panels are forward-looking UI with **honest empty states** ("arrives with R3 / R4") — no fabricated data; they wire to the R3 (transcription) + R4 (scripture-intelligence) epics later. An independent adversarial review found **0 high/medium** (JS wiring, controls, the emergency safety-invariant, honest placeholders, layout all clean); the 1 low worth closing (an `#outputs` overflow guard) was fixed. Pinned keymap + token tests green (test_tokens gained the OBS-zone + honest-panel pins); a headless-Chrome render matches the design; workspace + fmt green; CI green. Handled per the `/build` `frontend-engineer` skill + the `/goal` engine (validator `--require-complete` PASS). Story `86ajpztgr` → QA. **Batch 8b (Text shaping) is parked (recon only, no code) — resume next.**

## Stage 8 outcome — batch 8b (Text shaping, S8-2) — code-complete, BLOCKED on CI (2026-07-25)

**Real Unicode text shaping now replaces the ASCII-only bitmap font — "María" renders the í and Yoruba/Igbo tonal marks shape correctly (FR-017); executed through the backend-engineer skill + the `/goal` engine, per ADR-0014.** `selahcue-engine::raster::draw_text` shapes each `Layer::Text` line with **cosmic-text + rustybuzz** (pure-Rust HarfBuzz over ttf-parser) and rasterizes glyphs with **swash**, over a single **bundled OFL font** (Noto Sans, Latin subset, 127 KB) — system fonts are never loaded, so text shapes/rasterizes byte-identically on every OS (NFR-014) with a memory-safe parser (FR-173, no C FFI). The GPU compositor renders Fill only and CPU-rasterizes text via this path, so all output text is fixed; the GPU SSIM oracle (Fill) is unaffected.

**The adversarial review (`wf_6640e4b1-bf5`, 4 verified lenses) was worth it — 15 raised → 9 confirmed → all fixed** (deduped to 3 roots + 1 low): a **HIGH** unbounded-text-work regression (a long line rasterized every glyph with no off-screen culling → per-frame stall; fixed with a visible-glyph cull loop that breaks at `clip_right`), a **MEDIUM** descender/dot-below crop of the exact Yoruba/Igbo marks (fixed by sizing the font to 0.72·px so the ~1.36em glyph box fits the cell), a **MEDIUM** no-leak cache-growth (fixed by rebuilding the thread-local caches every 4096 renders), and a **LOW** non-saturating arithmetic. Each has a regression test (16 raster tests total; the safe-margin overscan invariant is now stronger). **Full workspace (48 suites) + GPU parity green; fmt + clippy clean; `--features server` E2E green.**

**BLOCKED on CI (owner action):** the CI-gated criteria — 3-OS byte-parity, the dependency-audit + supply-chain/license jobs (which must vet the new cosmic-text deps + the OFL font), and CI-green — **cannot run**: GitHub Actions is not dispatching jobs; every job (including the Rust-independent Flutter job) completes with **zero steps and no logs** (runs 30160556745, 30161697707) — a **repo-level Actions outage** (exhausted minutes/spending limit or Actions disabled), not batch 8b (which passes every check locally). The new deps are MIT/Apache + OFL (all allowlisted in `deny.toml` by inspection); `cargo audit` tolerates unmaintained advisories — but the jobs must actually run to confirm. **Owner: check GitHub → Settings → Billing & plans / Actions**, then re-run to close C-003/005/006. **UPDATE (Actions restored): CI run 30165455037 (HEAD contains this batch) is fully green (12/12) — rust ×3 OSes (cross-OS byte-parity ✓), dependency-audit + supply-chain ✓ (the cosmic-text deps + OFL font pass). All 6 criteria PASS; batch 8b is VERIFIED_COMPLETE; story `86ajpzha7` → QA.** Handled per the `/build` `backend-engineer` skill + the `/goal` engine.

## CI cost reduction — path-filter the matrix (`86ajq0569`) (2026-07-25)

**Owner-requested (the repo hit its GitHub Actions quota this session): cut CI runner-minutes without losing coverage.** `.github/workflows/ci.yml` gained a `changes` job (dorny/paths-filter) that gates the rest by which side changed — a **desktop** change runs the full 3-OS rust/operator/launch-smoke/audit/supply-chain matrix; a **mobile** change runs only Flutter; the heavy Android `apk --debug` step runs only when the Android host changed; every filter includes `ci.yml` so a workflow change re-runs everything. **No matrix OS, gate, or step was removed** — the 3-OS parity gate is preserved on every desktop change; caching + the docs-only skip are retained. Since macOS bills 10× and Stage-8 commits are overwhelmingly single-side, this roughly **halves** average weighted-minute consumption. Reviewed (0 high/med; 3 LOW fixed: a web/** catch-all so no code area is silently unverified, pubspec in the android filter, and a corrected header comment flagging that an aggregator job is needed before enabling branch-protection required checks). YAML-valid + DAG-audited + scenario-traced locally; confirmed green by CI run 30165455037 (the `detect changed areas` path-filter job + full matrix all passed). Handled per the `/build` `devops-engineer` skill + the `/goal` engine (validator `--require-complete` PASS).

## Stage 8 outcome — theme system (S8-3a design · S8-3b engine · S8-3c designer UI) (2026-07-26)

**FR-010 "theme editable centrally" delivered end-to-end.** After the owner correction that a SelahCue theme is a **slide-design template** (typography/alignment/background/positioned regions — like ProPresenter/Pewbeam), *not* a light/dark colour mode, the S8-3 story was decomposed and rebuilt design-first:
- **S8-3a — theme-model spec** (`86ajq14w4`, `docs/design/THEME-MODEL-spec.md`): a `Theme{background, title, body}` of positioned `RegionStyle`s (per-mille rect + H/V alignment + typography + `Fit`), with **shrink-to-fit the default** so content ⟂ theme = zero content loss.
- **S8-3b — render engine** (`86ajq14vq`): the compositor honours per-region rect%, real horizontal + vertical alignment (measured cosmic-text runs), per-region size/colour/line-height, and `Fit`; `Presenter::set_theme` recomposes Preview + Live from the **retained** slides (zero content loss); 3 built-ins (classic/high-contrast/lower-third); additive `SetTheme` wire + migration v8 + recovery.
- **S8-3c — Theme Designer editor UI** (`86ajq14wa`): the operator console gained a Theme Designer surface (app menu → Theme Designer) — a theme list + "New (from current)", an **accurate host-rendered `<canvas>` live preview** (`preview_theme` composes a John 3:16 sample via the *same* `compose_slide → engine::render` path as the audience output → base64 RGBA → `ImageData`, debounced; no host round-trip, no PNG dep), and an Inspector (per-region colour · size · alignment · line-height · **Fit** + background). **Apply** sends the additive `SetCustomTheme{theme_json}` (Operator-only RBAC), persisted via **migration v9** (canonical re-serialized → bounded) + crash recovery with custom-over-named precedence. The console theme picker + outputs panel were removed (managed on the Screens surface); pinned invariants (emergency footer/keymap, token needles, structure test) intact.

**Independent adversarial review** of S8-3c (`wf_a55ca48d-b5d`, 4 lenses → per-finding verify, 15 agents): 11 raised → **4 confirmed (2 MED a11y — segmented-control roles/labels + Apply-failure surfacing; 2 LOW test-gaps), all fixed; 7 refuted** (incl. a verbatim-JSON persistence gap pre-empted before the verify pass). Full workspace test + fmt + clippy clean; operator `cargo check` clean; headless surface render inspected. See `docs/delivery/CODE-REVIEW-batchS83c.md`. Handled per the `/build` `frontend-engineer` skill + the `/goal` engine (validator `--require-complete` PASS). Story `86ajq14wa` → QA.

## Stage 8 outcome — S8-3c refine: Theme Designer to the Figma (204-124/208-137) (2026-07-26)

**Owner refine bringing the Theme Designer up to the approved Figma**, handled per the `/build` `frontend-engineer` skill + the additive theme-engine work it needed (`/goal`, `TASK-86ajq14wa-theme-designer-refine.md`, validator `--require-complete` PASS):
- **Full-center canvas** (Figma 204-124): left Templates · a large 16:9 center canvas with a 5% safe-area guide · right inspector split into **LAYOUT** (region · X/Y/W/H% · horizontal + vertical alignment) + **TYPE** (bg/text colour · size · line-height · Fit).
- **On-canvas resize/reposition**: an 8-handle selection box over the canvas — drag moves the region, handles resize it (opposite edge anchored), numeric X/Y/W/H edit it, all clamped to the frame + live-previewed; keyboard-operable (arrows nudge, Shift+arrows resize). Built-ins are now **host-sourced** (`builtin_themes`), removing the hand-mirrored JS copy that would drift from `theme.rs`.
- **Lower-third → full-width band** (Figma 208-137): an **additive** theme-engine `Band` (`Theme.band: Option<Band>`, `Layer::Fill` × alpha) draws a translucent panel + amber border behind the text — the lower-third is now a full-width bottom bar (reference amber above white body inside the band), not a narrow left column. Full-screen themes keep `band: None` (pixels unchanged; serde additive/back-compat).
- **File split**: `dist/index.html` split into index.html (structure) + app.css (styles) + app.js (logic), served locally from `frontendDist: "dist"` — no runtime overhead; `app.js` is now `node --check`-able; the pin tests read the combined sources and keep the emergency-footer/keymap invariants.
- **Edit/save default templates** (the Figma "Save changes") → registered as a **follow-up story `86ajq4xmy`** (saved-theme library: Save changes · duplicate · rename · delete), not built here.

**Independent review** (`wf_9ab417e1-53a`, 4 lenses → per-finding verify, 7 agents): 3 raised → **3 confirmed (2 MED + 1 LOW, all in drag/resize), all fixed + re-verified; band/lower-third/split lenses clean.** Fixes: resize is edge-based so the anchored edge never jumps; numeric fields commit on `change` (multi-digit entry works); a `pointercancel` handler clears a cancelled drag. Full workspace test + fmt + clippy clean; operator clippy + build clean; headless full-center render + a headless interaction test (keyboard/drag/resize/numeric/cancel); rendered lower-third matches 208-137. See `docs/delivery/CODE-REVIEW-batchS83c-refine.md`. Story `86ajq14wa` → QA. **Open S8 follow-ups:** saved-theme library (`86ajq4xmy`, new), S8-3d per-item override (`86ajq14wn`), per-screen theme engine (`86ajq321k`), theme enhancements R-later (`86ajq3225`), App-menu + Screens page frontend impl.
## Stage 8 outcome — S8-3c refine 2: shrink-to-fit width fix + Figma 204-124 honest-shell alignment (2026-07-26)

**Two owner-reported issues on the Theme Designer**, handled per the `/build` `frontend-engineer` skill + the render-engine fix it needed (`/goal`, `TASK-86ajq14wa-theme-designer-refine2.md`, validator `--require-complete` PASS):
- **Shrink-to-fit clipped long scriptures** — root cause: `compose::layout_region` sized the cell to fit the line COUNT (height) only, and `raster::draw_text` shapes each line unwrapped, so a line wider than the region clipped on the right. Fix: added `raster::measure_line_width` (mirrors `draw_text`'s font sizing) and a bounded width-shrink pass in `layout_region` that scales the cell so the **widest** line also fits `rect.w`. A long verse now renders fully (shrinking for width only adds vertical room, so the line-count guarantee holds). Deterministic; existing render/shrink tests stay green.
- **Aligned the editor to Figma 204-124** (owner: honest shell + keep the 3 built-ins) — added the header (New wired · Import/Export/Save changes), Scriptures/Slides tabs, the Add-content toolbar, the CANVAS label, a **functional 9-point alignment icon grid**, a **functional Lock-aspect** resize constraint, and Ref-gap + Font/Weight/Letter as **disabled, honestly-labelled** placeholders (→ the multi-font engine `86ajq3225`). Fit reordered to Shrink/Paginate/Clip. Deferred features are honest dimmed "later" affordances (no fake success — Import/Export/Save route to the saved-theme library `86ajq4xmy`).

**Independent review** (`wf_01c8ca19-a5a`, 3 lenses → per-finding verify, 5 agents): 2 raised → **2 confirmed (both LOW a11y), both fixed; the shrink-fit + design-honesty lenses found nothing real.** Fixes: `Save changes` failed WCAG-AA text contrast under the dimming (green + 0.55 opacity) → dropped the green so the dimmed label passes (Apply stays the green primary); the Slides tab was an incomplete ARIA tabs widget → added `role=tabpanel` + `aria-controls` + `aria-disabled`. Full workspace test + fmt + clippy clean; operator clippy + build clean; headless render + a lock-aspect interaction test. See `docs/delivery/CODE-REVIEW-batchS83c-refine2.md`. Story `86ajq14wa` → QA. **Open S8 follow-ups:** saved-theme library (`86ajq4xmy`), multi-font + reference-gap (`86ajq3225`), Slides templates + on-canvas add-content, S8-3d per-item override (`86ajq14wn`), per-screen theme engine (`86ajq321k`).

## Stage 8 outcome — auto-fit verse text (stop truncating long verses; wrap-to-width + auto-size) (2026-07-26)

**Owner-reported: Esther 8:9 (the longest KJV verse) rendered TRUNCATED with "…" and didn't fill the box.** Handled per the `/build` `backend-engineer` skill + the `/goal` engine (`TASK-86ajq14wa-autofit-text.md`, validator `--require-complete` PASS):
- **Root cause:** the controller pre-wrapped verses at a fixed 42 columns and **capped at 6 lines with "…"** (`SCRIPTURE_MAX_LINES`) — a stale leftover from before the theme engine's shrink-to-fit existed.
- **Fix (zero content loss, FR-010):** the controller now passes the full verse (scripture) / stanza lines (songs) as **paragraphs**; `compose::layout_region` **word-wraps** each paragraph to the region width and **auto-sizes** the font via a binary search for the largest cell (≤ the design size) where every wrapped line fits BOTH the region height and width. A long verse shrinks so the whole passage shows and fills the box; a short one keeps the design size; song stanza breaks are preserved. Esther 8:9 now renders all 8 wrapped lines ending "language." — no "…".

**Independent review** (`wf_f491e24f-6ed`, 3 lenses → per-finding verify, 9 agents): 6 raised → **2 confirmed (1 HIGH + 1 MED), both fixed; 4 refuted.** (HIGH) the wrap measured the accumulated line per word → O(words²) shaping, unbounded for a large single paragraph → the SYNCHRONOUS Go Live could stall for seconds mid-service → rewrote the wrap O(N) memoized per-word + a `MAX_WRAP_WORDS` budget (a 3000-word paste now composes ~19.5ms, was multi-second). (MED) an over-wide unbreakable token (a very long word, or a space-less CJK verse) clipped on the right because the fit check ignored width → the predicate now checks width and shrinks the cell until it fits. Added tests: the long-paragraph wrap+fit, the unbreakable-token width shrink, the full-passage-no-truncation (Esther/Psalm 119/John 11:35), and a long-verse go-live latency test. Full workspace test + fmt + clippy + operator build clean. See `docs/delivery/CODE-REVIEW-batch-autofit.md`. Story `86ajq14wa` → QA. **New seams:** full `Fit::Paginate` (multi-slide) + per-character CJK line-breaking. **Open S8 follow-ups:** saved-theme library (`86ajq4xmy`), multi-font + reference-gap (`86ajq3225`), S8-3d per-item override (`86ajq14wn`), per-screen theme engine (`86ajq321k`).

## Stage 8 outcome — stage/confidence-monitor auto-fit (parity with the audience) (2026-07-26)

**Owner: apply the shrink-to-fit (auto-fit) to the STAGE output too.** The audience output already auto-fits, but the stage / confidence monitor (`compose_stage`/`push_region`) used its own fixed top-anchored `layout_lines` that truncated (dropped lines past the region bottom) + clipped long verses. Handled per the `/build` `backend-engineer` skill + the `/goal` engine (`TASK-86ajq14wa-stage-autofit.md`, validator `--require-complete` PASS):
- **Refactor:** extracted the audience auto-fit core into a shared `compose::autofit_layers(lines, rect, max_cell, lh, align_h, align_v, color, fit)`; `layout_region` is now a thin wrapper — the audience output is byte-identical.
- **Stage:** `stage::push_region` calls `autofit_layers` (ShrinkToFit, top-anchored) so the confidence monitor word-wraps + shrinks the full slide — the speaker sees the whole verse (reference + full text), never truncated/clipped. Removed the now-unused `layout_lines`/`LineMetrics`.

**Independent review** (`wf_f1436d1c-fe6`, 2 lenses → per-finding verify): **0 findings** — the refactor is byte-equivalent for the audience (compose/present suites green) and the stage change is clean (timer strip, current + next regions, clock, and identify overlay intact; geometry doesn't overflow/overlap; bounded work; dual-output independence holds). Full workspace test + fmt + clippy + operator build clean; a rendered stage (`scratchpad/stage.png`) shows the full Esther 8:9 on the confidence monitor. See `docs/delivery/CODE-REVIEW-batch-stage-autofit.md`. Story `86ajq14wa` → QA.

## Stage 8 outcome — S8-3d per-item theme override (completes the S8-3 theme umbrella) (2026-07-26)

**A plan item can now carry its OWN theme.** Handled per the `/build` `backend-engineer` skill + a small frontend picker (`/goal`, `TASK-86ajq14wn-per-item-theme.md`, validator `--require-complete` PASS). Additive vertical slice:
- **Model:** `PlanItem.theme: Option<String>` (a built-in name; `None` = the global theme).
- **Presenter (per-surface override):** `staged_theme`/`live_theme` alongside the global — Preview + Live can render DIFFERENT per-item themes at once; `go_live` promotes the staged override; a **global** `set_theme` recomposes each surface with its own override-or-the-new-global, so a global switch never clobbers an overridden item (FR-010 zero content loss both ways). The audience auto-fit `layout_region` is untouched.
- **Controller + wire + RBAC:** staging a plan item resolves `item.theme → Theme::builtin` (else global); `Command::SetItemTheme{item_id, theme}` sets/clears + reflects on the affected Preview/Live surface (marks the plan dirty for persistence); additive wire (v2 fixtures byte-stable) + RBAC `ConfigureOutputs` (Operator-only) + `PlanItemView.theme`.
- **Persistence:** migration **v10** (`plan_item.theme`, additive/nullable) + `plan_repo` round-trip; recovery restores an item's override (the live/staged item re-stages with its theme).
- **Frontend:** a per-item theme picker on each console plan row (an active override stays visible; blank = global).

**Independent review** (`wf_955aa2c9-f57`, 3 lenses → per-finding verify, 5 agents): 2 raised → **2 confirmed (1 HIGH + 1 MED), both fixed; the presenter-per-surface lens clean.** (HIGH) `set_item_theme` never marked the plan dirty → the override was never persisted → silently lost on restart → fixed (set the flag; the controller test now asserts `take_plan_dirty()`, closing the mask the original recovery test had). (MED) an open theme picker was torn out by the 1s poll during a running timer → fixed (defer the plan rebuild while the picker holds focus, mirroring the outputs deferral). Full workspace test + fmt + clippy + operator clippy/build clean; a headless plan render shows the picker + a live `lower-third` override. See `docs/delivery/CODE-REVIEW-batchS83d.md`. Story `86ajq14wn` → QA.

**S8-3 theme umbrella (86ajpzhak) is now COMPLETE:** S8-3a (design) · S8-3b (engine) · S8-3c (Theme Designer + refines) · S8-3d (per-item override). **Open S8 follow-ups:** per-screen theme engine (`86ajq321k`, next natural batch), saved-theme library (`86ajq4xmy`), theme enhancements R-later (`86ajq3225`), App-menu + Screens frontend impl.

## Stage 8 outcome — saved-theme library (86ajq4xmy): persist NAMED custom themes (2026-07-26)

**The Theme Designer now has a real named-theme library** — the Figma "Save changes" + a template list of built-ins **plus** saved themes (load · delete · Apply). Handled per the `/build` `backend-engineer` + `frontend-engineer` skills via the `/goal` engine (`TASK-86ajq4xmy-saved-theme-library.md`, validator PASS). Additive vertical slice:
- **Store (controller):** `saved_themes: BTreeMap<String,String>` (name → **canonical** theme_json) + a `saved_themes_dirty` flag + `save_theme`/`delete_theme`/`saved_themes`/`take_saved_themes_dirty`/`mark_saved_themes_dirty`/`load_saved_themes`. Bounded: `MAX_SAVED_THEMES=256` + `MAX_THEME_NAME_LEN=64` (bytes); a NEW name past the cap is rejected, invalid JSON / empty name rejected, the stored value is the canonical re-serialized `Theme` (never raw wire bytes). `load_saved_themes` drops invalid/over-cap entries defensively without dirtying.
- **Wire + RBAC:** additive `Command::SaveTheme{name,theme_json}` + `Command::DeleteTheme{name}` + `OperatorStateView.saved_themes: Vec<SavedThemeView{name,theme_json}>` (skip-if-empty → pinned v2 fixtures byte-stable) → RBAC `ConfigureOutputs` (Operator-only).
- **Persistence:** migration **v11** (`saved_theme` table; `target_version()==11`, older DB opens empty) + a transactional `saved_theme_repo` (`save_all` DELETE+INSERT one-tx / `load_all` ordered) + desktop load-on-startup (config, so it loads on clean/crash paths) + save-on-`take_saved_themes_dirty()` in the autosave loop **and** the clean-exit flush (re-arms on a failed write).
- **Frontend:** the Theme Designer library — a real **Save changes** inline name form (no `window.prompt`; WKWebView-safe), the template list = built-ins (read-only, tagged) + saved themes (load / two-click ✕ delete), wired to host `save_theme`/`delete_theme`; the list folds `view.saved_themes` on each poll (guards a focused save-name field). New Tauri command wrappers + `OperatorShell`/`RemoteOperator` methods.

**Independent review** (`wf_f5b8ac93-041`, 4 lenses → per-finding verify, 9 agents): 5 raised → **3 confirmed (2 MED + 1 LOW, all frontend), all fixed; 2 refuted.** (MED) a host-**denied** save reported fake success because a DENY resolves `Ok(unchanged view)` → now verifies the returned view actually contains the name + rejects an over-64-**byte** name client-side (the input's `maxlength=64` counts UTF-16 units, not the host's 64 bytes). (MED) the two-click delete-confirm was defeated by a mid-confirm list rebuild → `tdConfirmDel` is cleared on every rebuild. (LOW) a saved theme sharing a built-in's name double-highlighted → selection is keyed by `{kind, name}`. **Plus a serialization bug the review missed but the batch's own headless render caught:** the internal `OperatorView.saved_themes` serialized as `[[name,json]]` tuples, not the `{name,theme_json}` objects the webview reads → switched to `Vec<SavedThemeView>` + pinned the shape with a `serde_json::to_value` assertion. Full workspace test 329/0 + fmt + clippy clean; operator clippy/build clean; headless render + scripted Save→list→delete + the three fix-verifications. See `docs/delivery/CODE-REVIEW-batch-saved-themes.md`. Story `86ajq4xmy` → QA. **Open S8 follow-ups:** `RenameTheme`/duplicate ops + import/export a theme file (deferred seams), per-item/per-screen use of a saved theme (`86ajq14wn`/`86ajq321k`), multi-font + reference-gap (`86ajq3225`), App-menu + Screens frontend impl.

## Stage 8 outcome — per-screen theme map (86ajq321k): each Audience screen renders its own template (2026-07-26)

**Unblocked + implemented the per-screen theme engine** (owner chose "unblock per-screen theme (multi-output infra)" at the gate). The theme engine now supports a **per-screen theme map** so each Audience-class screen (`main` projector, a `lower-third` feed, a `stream` feed) renders its OWN template SIMULTANEOUSLY from the same live content — the prerequisite the Screens page (86ajq321f, in QA) needed (its per-screen Theme pickers had wired to the global `set_theme` as a placeholder). Handled per the `/build` `backend-engineer` skill + `/goal` (`TASK-86ajq321k-per-screen-theme.md`, validator PASS). Additive vertical slice:
- **Engine (present):** the Presenter gained `main_screen_theme` + an `effective(item)` = **per-screen ▸ per-item (S8-3d) ▸ global** helper (so `main` with no per-screen theme is byte-identical to before — a regression-guard test) + `compose_screen_live(theme)` which renders the current live slide on-demand under any theme (for the SECONDARY screens; a blank live → black).
- **Controller + wire + RBAC:** `screen_themes` map (bounded to `AUDIENCE_SCREENS = [main, lower-third, stream]`, **built-in names only** like per-item) + `compose_screen(screen)` (blackout-aware); additive `Command::SetScreenTheme{screen,name}` + `OperatorStateView.screen_themes` (skip-if-empty, v2 fixtures byte-stable) → `ConfigureOutputs` (Operator-only). The internal view serializes `screen_themes` as `{screen,theme}` objects.
- **Persistence + recovery:** migration **v12** `screen_theme` table + transactional repo + desktop load-on-startup (after the library + after restore) + save-on-dirty (autosave loop + clean-exit flush).
- **Frontend:** the Screens page's audience Theme picker → `set_screen_theme{screen}` (not the global), reflecting each screen's theme, with a "◈ Follow global" clear; virtual `lower-third`/`stream` rows each get their own theme; physical NDI/stream OUTPUT delivery is the honest remaining seam. The main window renders `main`.

**Decision (recorded):** effective theme = per-screen ▸ per-item ▸ global; `main` byte-identical when unset; a per-screen theme is **built-in-only** (mirrors `set_item_theme`, keeps `main`'s cached theme from going stale when the library changes). The main audience output is the only physical window this batch; secondaries compose on-demand — physical multi-output (NDI/stream) transport, saved-per-screen themes, and add/delete-virtual-screen are honest follow-up seams.

**Independent review** (`wf_89bb043f-d79`, 4 lenses → per-finding verify, 6 agents): 2 raised → **2 confirmed (both MED), both fixed; 0 refuted.** (engine) `main`'s cached per-screen theme could go stale if it referenced a saved-library theme later edited/deleted → restricted to built-in names only. (frontend) no "follow global" option meant a per-screen theme couldn't be cleared from the UI → added the blank clear entry. Full workspace test **341/0** + fmt + clippy clean (workspace + operator); headless Screens render (3 distinct per-screen themes) + scripted picker-selection check. See `docs/delivery/CODE-REVIEW-batch-per-screen-theme.md`. Story `86ajq321k` → QA. **S8-3 theme umbrella + this engine follow-up now cover the full per-screen theme model.** **Open follow-ups:** physical secondary-output delivery (NDI/stream, R-later), saved-per-screen themes, add/delete virtual screen + enable/disable, theme enhancements R-later (`86ajq3225`).

## Stage 8 outcome — saved themes usable per-item + per-screen (86ajq69ft) (2026-07-26)

**A custom theme saved in the Theme Designer can now be applied per-item AND per-screen, not just globally** (owner chose "saved themes everywhere" at the gate). Closes the built-in-only seam from S8-3d (per-item) + 86ajq321k (per-screen), and adds the re-sync-on-library-change path the per-screen review flagged. Handled per the `/build` `backend-engineer` skill + `/goal` (`TASK-86ajq69ft-saved-themes-everywhere.md`, validator PASS). New story `86ajq69ft` (created under Presentation epic 86ajp07ce; none existed). Mostly controller + a thin frontend wire — **no wire command or migration change**:
- **Resolution:** a free `resolve_theme_name(name, &saved_themes)` (built-in ∪ saved-library JSON) used by `set_item_theme`/`set_screen_theme`/`stage_slide`/`compose_screen`/`load_screen_themes` — accept a built-in OR a saved name; a truly-unknown name is rejected. Built-in path unchanged.
- **Re-sync (`resync_theme_overrides`):** on `save_theme` (edit) + `delete_theme` (delete) — drops dangling per-item/per-screen references (deleted → falls back to global), then re-applies the `main` screen theme + the LIVE item override + re-stages the STAGED item (blackout re-asserted). An edit reaches the live output; a delete leaves no stale frame.
- **Recovery:** the desktop loads the library + per-screen map BEFORE `restore()`, so saved overrides resolve as content re-stages.
- **Frontend:** the per-item + per-screen Theme pickers list built-ins + a "Saved" optgroup from `view.saved_themes`.

**Independent review** (`wf_d2389630-37f`, 3 lenses → per-finding verify, 4 agents): 1 raised → **1 confirmed (LOW), fixed; 0 refuted** — a saved theme named exactly like a built-in was silently shadowed (built-ins resolve first, so it was unreachable by name); `save_theme` now reserves built-in names (rejects them, mirrored client-side) + a test. Full gate green (fmt/clippy clean on every target this batch touches; operator builds); 384/0 workspace tests; headless render (both pickers show the Saved group + reflect the current saved selection). See `docs/delivery/CODE-REVIEW-batch-saved-themes-everywhere.md`. Story `86ajq69ft` → QA.

**The theme system is now coherent end-to-end:** design a custom theme (Theme Designer) → save it to the library (86ajq4xmy) → apply it globally, per-item (86ajq14wn), or per-screen (86ajq321k). **Note:** a separate in-progress QA test-programme (86ajq67q2) is adding comprehensive test files in the tree; those are unrelated to this batch. **Open follow-ups:** import/export a theme file, add/delete virtual screen + enable/disable, physical secondary-output (NDI/stream) transport (R-later), theme enhancements R-later (86ajq3225).

## Stage 8 outcome — selectable system fonts (86ajq6fxt) (2026-07-26)

**A theme can now select a font installed on the render machine** (owner: "make the fonts selectable; use the existing fonts on the computer; no need to bundle fonts"). Split from Theme engine enhancements 86ajq3225 into its own story. Handled per the `/build` `backend-engineer` skill + `/goal` (validator PASS). The engine deliberately bundles ONE font (Noto Sans) + disables system fonts for cross-OS determinism (NFR-014; the parity + pinned-render tests rely on it) — so system fonts are made **opt-in**, keeping Noto Sans the **deterministic default**:
- **Engine:** `scene::Layer::Text` + `Theme.font: Option<FontName>` (a `Copy` bounded `arrayvec::ArrayString<64>`, so `Theme` stays a fixed-size POD; a validating serde routes wire values through `FontName::new`). `draw_text`/`measure_line_width` render the selected font via a LAZY system `FontSystem` (`load_system_fonts`) or the unchanged bundled shaper for the default (byte-identical → NFR-014 preserved). A missing font degrades to a readable platform font (never tofu/panic); bounded (periodic reset). `system_font_families()` enumerates (sorted/deduped/bounded).
- **No wire command, no migration:** the font rides in the existing theme JSON (`SetCustomTheme`/`SaveTheme`, saved-theme table, session custom_theme) — `target_version` unchanged, pinned fixtures byte-stable.
- **Frontend:** the Theme Designer "Font" control is a real picker from `system_fonts()` (default entry clears it; an un-installed saved-theme font shows "(not installed here)").

**Independent review** (`wf_a5313bef-b7e`, 3 lenses → per-finding verify, 7 agents): 4 raised → **3 confirmed (1 MED + 2 LOW), all fixed; 1 refuted** — a serde bypass that let `{"font":""}` masquerade as a themed default (→ validating deserialize), a dead/false "Noto Sans fallback" claim (→ corrected to "readable platform font"), and a picker that blanked an un-installed font (→ reflects it). Determinism gate green incl. **test_parity**; fmt/clippy clean; headless font-picker checks. See `docs/delivery/CODE-REVIEW-batch-selectable-fonts.md`. Story `86ajq6fxt` → QA.

**Follow-up:** the owner's expanded IMAGE vision (add image · z-order/send-to-back · opacity · resize) is now a first-class **EPIC — Canvas Editing & Composition (86ajq6j01)** with 5 stories (design · layered element model + z-order · image element · on-canvas interactions · text/shape elements) + dependencies wired. 86ajq3225 rescoped to gradient backgrounds + pagination. **Open theme follow-ups:** multi-weight/italic fonts + per-region fonts (86ajq6fxt seams); the Canvas Editing epic.

## Stage 8 → Canvas Editing — layered element model + z-order + opacity (86ajq6j2q) (2026-07-27)

**The engine foundation of the Canvas Editing epic (86ajq6j01):** the render/theme model now carries an **ordered list of layered elements**, each with a per-mille rect + **opacity** + a **z-index** relative to the text, composited back-to-front. This batch delivers the model + compositing + the **shape** element kind (fill/border, a generalized `Band`). Handled per the `/build` `backend-engineer` skill + `/goal` (`TASK-86ajq6j2q-element-model.md`, validator PASS 4/4). **Key architecture finding:** a shape element composites to the existing `Layer::Fill` (exactly as `Band` does) — so **no `scene`/engine/GPU change** was needed, and the `Theme: Copy → Clone` ripple was just **3 sites**. Additive — **no wire command, no migration**:
- **Model (present/theme):** `Theme.elements: Vec<Element>` (`skip_serializing_if` → a default theme's JSON is byte-identical); `Element::Shape { rect permille, fill, border, border_permille, opacity, z }` (all-scalar → `Copy`); `Theme` derives `Clone` (RegionStyle/Band/FontName/Element stay `Copy`); bounded `MAX_ELEMENTS = 64`.
- **Compose (present):** `element_layers` renders a Shape as `Layer::Fill` layers (fill + up to 4 border edges) with opacity multiplied into fill+border alpha; `compose_slide` stable-sorts by `z` and renders `z<0` **behind** the text, `z>=0` **in front**. A no-element theme is byte-identical (determinism preserved — parity + pinned green).
- **Persistence + bound:** elements ride in the theme JSON (`custom_theme`/`saved_theme`); `set_custom_theme`/`save_theme` **and** the startup `load_saved_themes` reject `elements.len() > MAX_ELEMENTS`.

**Independent review** (`wf_8475bc43-098`, 3 lenses → per-finding verify, 7 agents): 4 raised → **3 confirmed (2 MED + 1 LOW), all fixed; 1 refuted.** The two MEDIUMs were the SAME defect from two lenses — `load_saved_themes` was the one theme-with-elements ingress not bounded by `MAX_ELEMENTS` (write paths were, the startup store load wasn't → a tampered/version-skewed DB row could smuggle an unbounded element Vec into compose) → the load filter now bounds `elements.len()` too, dropping over-cap rows defensively. The LOW: element borders now carry the shape opacity, so overlapping full-length edges double-blended (brighter) at the four corners → left/right edges own the corners, top/bottom cover only the interior width, so every border pixel blends once. Refuted: an unknown future element `kind` dropping the whole theme on an older build — inherent to the additive-enum seam, and the intended honest degradation. Re-verified `make ci` **ALL GREEN** (workspace fmt/clippy/test incl. **test_parity** + operator + Flutter). See `docs/delivery/CODE-REVIEW-batch-element-model.md`. Story `86ajq6j2q` → QA.

**Deferred with seams (dependent stories):** the **image** element kind + its sandboxed decode (`86ajpzhbc`/`86ajq6j49`), the **text** element kind, and all on-canvas authoring UI — add/select/drag/resize/**send-to-back** (`86ajq6j4p` interactions, `86ajq6j64` text/shape). This batch is the engine model + compositing + tests, not the Designer UI. **Open follow-ups:** the Canvas Editing epic stories above; gradient backgrounds + pagination (`86ajq3225`).

## Stage 8 → Media / Canvas Editing — image decode foundation (S8-6, 86ajpzhbc) (2026-07-27)

**The reusable image render foundation** the Canvas Editing image element (`86ajq6j49`) depends on: a bounded, deterministic, panic-contained **in-process PNG decode** feeding a new additive **`scene::Layer::Image`** on the deterministic CPU raster path, with a **missing-media placeholder** (FR-070) and decoder-**fault isolation** (NFR-024). Handled per the `/build` `backend-engineer` skill + `/goal` (`TASK-86ajpzhbc-image-decode-foundation.md`, validator PASS 6/6; scoped from a read-only 5-subsystem trace `wf_f9163b0e-40b`). **Architecture decision → ADR-0018:** bounded in-process decode NOW, refining ADR-0016's *sequencing* (its out-of-process sandbox stays the deferred end-state; fault isolation preserved, security isolation deferred). **No wire command, no migration, no RBAC change, no GPU-pipeline change** — the GPU skips `Layer::Image` exactly as it already skips `Text` (GPU-native images are a later batch), so the parity oracle is untouched (SSIM ≥ 0.99 unchanged) and CPU raster is the real render path.
- **Decoder:** the pure-Rust `png` crate (offline-available; the full `image` crate isn't cached). **PNG only** this batch — lossless + deterministic → byte-identical RGBA8 cross-OS (NFR-014); other formats degrade to the placeholder (a documented later-format seam). Header/type/allowlist validation + a **dimension AND byte cap enforced before allocation** (decompression-bomb-safe, FR-173); `Result`-based, panic-contained.
- **Model + blit:** `Layer::Image { rect, source: MediaRef (bounded/validated), opacity }` (additive serde, Fill/Text byte-stable, `Eq` preserved); `MediaRef` carries a **reference, never pixels**. Raster blits with integer nearest-neighbour scale + opacity + src-over (deterministic), resolving through a **bounded decode cache** (entries + byte budget, decoded pixels never in the serde `Frame` — no-leak).
- **Placeholder + fault:** a distinct **non-black** deterministic placeholder on any decode failure (missing/corrupt/unsupported/over-budget) — never blank, never crash; a `Fault::DecoderFault` holds the last-good frame with other outputs unaffected.

**Independent review** (`wf_d771a8fa-354`, 5 lenses → per-finding verify, 12 agents): 7 raised → **2 confirmed (same MED), fixed; 5 refuted.** The confirmed defect (both lenses): the new `draw_placeholder` did unchecked i32/i64 arithmetic on the unvalidated layer rect, so a crafted/missing image with an extreme rect overflow-**panicked** in debug/test builds — violating the batch's own FR-173/NFR-024 panic-containment (latent; all tests used tiny rects) → rewrote it overflow-safe (clipped-span coords in i64, diagonal metric in i128) + an adversarial-rect regression. The 5 refuted: arbitrary-file-read (deferred FR-138 seam, no wire path now), caller-limit bomb guard, sticky failed-decode cache (R2 seam), byte-cap coupling (holds, 160 MiB < 256 MiB), operator lock double-carry (premise wrong). Re-verified `make ci` **ALL GREEN** (workspace fmt/clippy/test incl. **test_parity** + operator + Flutter). See `docs/delivery/CODE-REVIEW-batch-image-decode.md`. Story `86ajpzhbc` → QA.

**Deferred with seams:** the theme **`Element::Image`** + on-canvas authoring — add/resize/opacity/**send-to-back** (`86ajq6j49` + interactions `86ajq6j4p`, design `86ajq6j29`) — now UNBLOCKED by this foundation; the out-of-process sandbox worker (ADR-0016 end-state); JPEG/WebP/GIF/BMP + animation; video/audio (S8-8); GPU-native image compositing; remote controller→host asset transfer; FR-138 canonicalization; cache invalidation/TTL. **Open follow-ups:** the image element `86ajq6j49` (the owner's image vision now has its decode foundation); gradient backgrounds + pagination (`86ajq3225`).

## Stage 8 → Canvas Editing — image element model (86ajq6j49 backend) (2026-07-27)

**The backend/model half of the owner's image vision:** a theme can now carry an **`Element::Image`** (per-mille rect · `MediaRef` · opacity · **z-order behind/in-front of the text** · resize-via-rect) that **composes → `scene::Layer::Image`** and renders on the audience output, persists in the theme JSON, and recovers. Handled per the `/build` `backend-engineer` skill + `/goal` (`TASK-86ajq6j49-image-element-model.md`, validator PASS 4/4). Consumes the S8-6 decode foundation (`Layer::Image` + bounded PNG decode + non-black placeholder). **No wire command, no migration, no RBAC change, no GPU-pipeline change** — the image element rides the opaque theme JSON; the GPU skips `Layer::Image` (like `Text`), so `test_parity` (SSIM ≥ 0.99, NFR-014) is unchanged.
- **Model:** `Element::Image { x/y/w/h_permille, source: MediaRef, opacity, z }` (reusing the engine's bounded/validated `MediaRef`); `Element` derives `Clone` (was `Copy`) since the image kind holds a heap-backed ref — **the Copy→Clone ripple cost ZERO extra sites** (`element_layers`/`compose_slide` take `&Element`). `MediaRef` re-exported from `selahcue-present`.
- **Compose:** an `Element::Image` arm → `Layer::Image` (per-mille→pixel rect + opacity); z-order + the empty-elements no-op inherited unchanged from `compose_slide`'s existing sort (an image slots in like a shape).
- **Persist + bound:** rides the theme JSON (`"kind":"image"`); `MAX_ELEMENTS=64` bounds the count on every ingress; decoded pixels never enter the theme/`Frame` (engine cache). A missing source → the non-black placeholder (never blank/crash); recovers deterministically.

**Independent review** (`wf_8d0322a5-977`, 3 lenses → per-finding verify): 1 raised → **1 confirmed (LOW, test-only), fixed; 0 refuted.** The model/compose/persistence lenses found no product defect. The LOW: the `temp_image()` test helper leaked PNG fixtures into the temp dir → fixed with an RAII `TempImage` guard (Drop removes the file). Re-verified `make ci` **ALL GREEN**. See `docs/delivery/CODE-REVIEW-batch-image-element.md`. Story `86ajq6j49` → QA.

**Deferred with seams:** all on-canvas authoring UI — add via file picker · select/drag/resize · **send-to-back/bring-to-front** (interactions `86ajq6j4p`, design `86ajq6j29`, Theme Designer image control); aspect-preserving `Fit` modes; the text element (`86ajq6j64`); other formats + the out-of-process sandbox (S8-6 seams); remote controller→host asset transfer. **Open follow-ups:** the on-canvas editing design `86ajq6j29` (the gate to making shapes + images visually authorable) + interactions `86ajq6j4p`; gradient backgrounds + pagination (`86ajq3225`).

## Stage 8 → Canvas Editing — element authoring UI (86ajq6j4p) (2026-07-31)

**The Canvas Editing epic is now USABLE:** shapes + images can be **added, selected, dragged, 8-handle-resized, arranged (send-to-back / bring-to-front), opacity-adjusted, and deleted** on the Theme Designer canvas, with a per-element inspector — the interaction layer over the shipped element model. Handled per the `/build` `frontend-engineer` skill + `/goal` (`TASK-86ajq6j4p-canvas-interactions.md`, validator PASS 4/4; scoped from a read-only scout of the operator webview). **Client-side only** (`selahcue-operator/dist/{app.js,index.html,app.css}`) — **no host change, no new dependency, no engine/wire/migration change.**
- **Key finding (scout):** the host already composites `tdTheme.elements` (via `render_sample`) + persists them (via `SetCustomTheme`, bounded at 64), and the region drag/8-handle machinery already works on any per-mille rect — so this **generalised `tdRegion` to an active-target (region OR element)** with zero host work. The preview auto-renders added elements.
- **Interactions:** Add Shape / Add Image (host-path input — the native OS file-picker + FR-138 confinement are the offline-blocked `86ajq6j49`-frontend follow-up); click hit-test select; drag-move + 8-handle resize (reused, pointercancel-safe); **arrange rewrites the `z` value** per CANVAS-EDITING-spec §2a (compose sorts by z, so a list-reorder would be a no-op) with a behind/in-front chip; two-click delete; keyboard (arrows nudge, Shift+arrows resize, Cmd/Ctrl+]/[ arrange, Delete, **Escape deselect**). Per-element inspector: X/Y/W/H, opacity (↔u8), shape fill/border/border-width, image source. a11y (ARIA handles, aria-live announcements). Preview-only invariant intact.

**Independent review** (`wf_dba0dc46-65a`, 3 lenses → per-finding verify, 15 agents): 12 raised → **11 confirmed, all fixed; 1 refuted.** MEDs: a two-click-delete arm that leaked across selection changes (→ reset on every selection change); no deselect path once an element was picked (→ Escape + empty-canvas deselect); the armed delete not announced to AT (→ tdAnnounce); the image path validated in UTF-16 units + NUL not rejected vs MediaRef's byte/NUL rule (→ tdNameBytes + NUL check). LOWs: arrange tie-handling + i16 clamp, handle/inspector ARIA, arrange announce-only-on-change. Re-verified: headless interaction check **22/22**; `node --check` clean; no Rust change (operator fmt clean). See `docs/delivery/CODE-REVIEW-batch-canvas-interactions.md`. Story `86ajq6j4p` → QA.

**Deferred with seams:** the native OS file-picker + FR-138 media-root confinement (`86ajq6j49`-frontend); multi-select/group; align-distribute + rotation (R2); the text element (`86ajq6j64`); aspect-preserving image Fit; undo/redo. **Canvas Editing epic status:** engine model ✅ · decode ✅ · image element ✅ · design spec ✅ · **authoring UI ✅ (this)** — the owner's image vision is now end-to-end usable (add a PNG by path → arrange behind/in front of text → set opacity → resize). Remaining: the native picker + more formats; the Figma frame set (design phase 2); the text element.

## Stage 8 → Canvas Editing — Theme Designer canvas refine (86ajq6j4p-refine) (2026-07-31)

**Owner refine cluster on the canvas authoring UI** (from an 8-item punch-list; the other 4 are tracked follow-ups): **#3** clicking an element on the canvas now SELECTS it (a defect — the selection box overlaid the canvas + blocked the hit-test → `tdHitTest` re-hit-tests on pointerdown); **#6** the per-item theme dropdown on the service plan is ALIGNED (fixed `width:104px`); **#4** a WKWebView-safe right-click **context menu** — Copy · Paste (offset clone, bounded 64) · Delete · Send-to-back · Bring-to-front (+ `Cmd/Ctrl+C`/`V`), keyboard-operable; **#1** a **native OS image picker** (`tauri-plugin-dialog` + an `async pick_image` command) replacing the host-path input (the path row remains a fallback). Handled per the `/build` `frontend-engineer` skill + `/goal` (`TASK-86ajq6j4p-canvas-refine.md`, validator PASS 4/4). Client-side (`dist/{app.js,index.html,app.css}`) + one small host command; **no engine/wire/migration change**. The picker dependency is **`cargo deny`-clean** (permissive, Tauri org).

**Independent review** (`wf_05171980-00b`, 3 lenses → per-finding verify, 8 agents): 5 raised → **4 confirmed, all fixed; 1 refuted** — including **2 HIGH the local checks couldn't catch**: (1) `.td-ctx { display:flex }` defeated the `[hidden]` attribute so the context menu never hid (the headless test checked the attribute, not the computed CSS) → added `.td-ctx[hidden]{display:none}`; (2) `pick_image` was a SYNC command → `blocking_pick_file` on the main thread would **deadlock/freeze the whole operator** mid-service (the build compiled + the headless test stubbed it) → made it **`async`**. Plus a MED (an overlapping element could steal a drag → sticky element grab) + a LOW (dismiss-click side-effect). Re-verified: operator rebuild + clippy + fmt + **deny** clean; **headless check 33/33** (incl. the menu's computed display + the dismiss click). See `docs/delivery/CODE-REVIEW-batch-canvas-refine.md`. Story `86ajq6j4p` → QA.

**Follow-ups registered** (owner refine items #2/#5/#7/#8): #2 more shape kinds (ellipse/line/rounded-rect + picker) → `86ajtwq24`; #7 operator Preview/Live panels show the true composited render → `86ajtwq28`; #8 scripture live-follow (owner decision: **only when already live**, preserving preview⟂live) → `86ajtwq2b`; #5 enable font weight/letter-spacing noted on the existing `86ajq3225`. **Canvas Editing epic:** engine model ✅ · decode ✅ · image element ✅ · design spec ✅ · authoring UI ✅ · **refine ✅ (this — native picker + context menu + click-select)**.

## Stage 8 → Canvas Editing — more shape kinds (86ajtwq24) (2026-07-31)

**Owner refine #2** — the Add Shape button could only make a rectangle; now the operator opens a **shape picker** (Rectangle / Ellipse / Rounded-rectangle / Triangle) and `Element::Shape` renders each **kind** — an area fill + optional inset-ring border, at the per-mille rect + opacity + z. Handled per the `/build` `backend-engineer` skill (engine raster + model + compose) + `frontend-engineer` (picker) via `/goal` (`TASK-86ajtwq24-shape-kinds.md`, validator PASS 5/5). Engine + present + operator-`dist/` only; **no wire command, no migration, no RBAC/GPU-pipeline change** — shapes ride the theme JSON and the GPU skips them (like `Text`/`Image`), so `test_parity` (SSIM ≥ 0.99, NFR-014) is unchanged.
- **Model:** `ShapeKind { Rect, Ellipse, RoundedRect, Triangle }` (engine `scene.rs`; `Copy`, `Default = Rect`, snake_case, `is_rect()`) + additive `Layer::Shape { rect, kind, fill, border, border_px, corner_px }` (`Eq`; zero border/corner skipped). `Element::Shape` gains `variant`/`corner_permille` via `serde(default, skip_serializing_if)` → **a rectangle shape's JSON is byte-identical to before** (no drift, no migration). `ShapeKind` re-exported from `selahcue-engine` + `selahcue-present`.
- **Raster:** `draw_shape` fills the parametric shape over the on-screen clipped span with a per-kind integer inside-test + an inset-ring border. **Ellipse = fixed-point i64** (`nx²+ny²≤SCALE²`, clamped) — **overflow-proof for every `u32` extent** (a degree-4 exact test would overflow even i128); **triangle** = exact i128 half-width; **rounded-rect** = exact i128 clamp-SDF. Pure integer → byte-identical cross-OS (NFR-014); bounded (extreme/`u32::MAX` rects clip, never overflow-panic — proven under `-C overflow-checks=on`).
- **Compose:** a `Rect` shape routes to the **verbatim-unchanged `Layer::Fill`** path (fill + 4 border edges); Ellipse/Rounded/Triangle → one `Layer::Shape` with opacity folded into the fill/border alpha and `border_px`/`corner_px` clamped to half the shorter side.
- **Operator (`dist/`):** a WKWebView-safe inline shape picker (`#td-shape-row`, keyboard/Esc, hides via the shared `.td-save-row[hidden]` guard); a Rectangle pick omits `variant` (byte-identical); Rounded gets a default `corner_permille` + a corner-radius inspector control (`#td-el-corner`, shown only for rounded); the inspector head names the geometry.

**Independent review** (`wf_20e389ba-d6a`, 3 lenses → per-finding refute-by-default verify, ultracode; 204k tokens / 51 tool calls): **0 raised → 0 confirmed** — a genuine clean pass (no analogue to prior batches' overflow/`[hidden]`/deadlock bugs, which this batch built directly over). The two highest-risk invariants were independently re-proven: **no debug-overflow panic** on `i32::MIN`/`u32::MAX` rects (+ `u32::MAX` border/corner) under an explicit `-C overflow-checks=on` build, and **byte-identical** rectangle serialization (new `test_compose`/`test_scene`) + the green `selahcue-data` migration suite (no fixture drift). Re-verified: `cargo test --workspace` **0 failures**; operator fmt/clippy/build + `deny bans·licenses·sources` clean; **headless 45/45** (10 new C-004). See `docs/delivery/CODE-REVIEW-batch-shape-kinds.md`. Story `86ajtwq24` → QA.

**Deferred with seams:** line / star / arrow / polygon kinds; per-shape gradients; arbitrary rotation; **GPU-native shapes** (with GPU glyphs/images); true uniform-inset borders (the inset-ring approximation is used). **Canvas Editing epic:** engine model ✅ · decode ✅ · image element ✅ · design spec ✅ · authoring UI ✅ · refine ✅ · **more shape kinds ✅ (this)**. **Open refine follow-ups:** #7 Preview/Live true render (`86ajtwq28`), #8 scripture live-follow (`86ajtwq2b`), #5 font weight/letter-spacing (`86ajq3225`).

## Stage 8 → Presentation — operator Preview/Live TRUE render (86ajtwq28) (2026-07-31)

**Owner refine #7** — the operator console's **Preview | Live** panels were text-only ("Nothing staged" / "Output idle" + a caption); they now draw the **real composited output the audience preview and live see** (background · theme · text · elements/shapes/images · **blackout** · the live scene), refreshed on state changes with a bounded payload. Handled per the `/build` `frontend-engineer` skill (+ a small backend render command) via `/goal` (`TASK-86ajtwq28-preview-live-render.md`, validator PASS 5/5). Engine + app + operator (`dist/` + one host command); **no wire/RBAC/migration change** — a **read-only** readback that never affects what is on air.
- **Design (Verified):** the Local backend's `OperatorShell` already composites `presenter().preview_output()`/`live_output()` (the true 1920×1080 audience frames, incl. blackout/live scene). Reading + downscaling them is more faithful than re-composing (which would drop blackout/timer/identify). The Remote backend receives indices/refs over TLS, not pixels → true render there is a **wire-streaming seam**; it degrades honestly to the text panel.
- **Engine:** `FrameBuffer::thumbnail(max_w,max_h)` — a deterministic integer **box-average** downscale (NFR-014), aspect-preserving, never-upscale, `max`-clamped/bounded.
- **App:** `OperatorShell::console_thumbnails(w,h)` — read-only (no command, no tick) → the two thumbnailed frames; a test asserts the live output is byte-identical before/after a render call (on-air unchanged).
- **Host + webview:** `render_console(w,h)` command (Local: base64 RGBA clamped 480×270; Remote: `{available:false}`); a `<canvas>` per panel drawn via `ImageData` (debounced 120 ms, console-gated, sig-deduped); `available:false`/error → the accessible text fallback.

**Independent review** (`wf_53f55c83-4ca`, 3 lenses → per-finding refute-by-default verify, ultracode; 5 agents, 277k tokens): **2 raised → 0 confirmed → 2 refuted.** Both were render-dedup-signature concerns: one was **already fixed proactively** (adding `view.items` so a slide-advance/per-item-theme change refreshes the panels — verified present in the current code); the other (`view.timer` forcing redundant every-second re-renders) was refuted as a sub-defect but **acted on** — the countdown is a stage-monitor-only overlay (`controller.rs` `tick`: "NOT on the audience/program output"), so `view.timer` was removed from the console signature (an efficiency win, no downside). Re-verified: `cargo test --workspace` 0 fail; operator fmt/clippy/build + `deny bans·licenses·sources` OK; **headless 51/51** (6 new #7). See `docs/delivery/CODE-REVIEW-batch-preview-live.md`. Story `86ajtwq28` → QA.

**Deferred with seams:** streaming the true pixels to a REMOTE controller (a LAN protocol change); per-output multi-screen thumbnails; a live video feed (this is a periodic still). **Refine follow-ups remaining:** #8 scripture live-follow (`86ajtwq2b`), #5 font weight/letter-spacing (`86ajq3225`); the deferred **line** shape kind (86ajtwq24).

## Stage 8 → Presentation — operator console render-FIX (86ajtwq28, owner QA) (2026-07-31)

**Owner QA on the real app found the shipped Preview/Live true-render did NOT display** (panels still showed the scripture reference text + rendered stretched). Three fixes, operator-only (`src/main.rs` + `dist/app.css`); no wire/RBAC/migration change. Handled per the `/build` `frontend-engineer` skill + `/goal` (`TASK-86ajtwq28-console-render-fix.md`, validator PASS 4/4).
- **#3 render-not-applied (host-side):** `render_console` was the ONLY sync `#[tauri::command]` taking `State` + returning a bare `serde_json::Value` (every working command is `async`→`Result`). A diagnostic with a REAL `view` stub proved the JS boot path fires the render + applies `.has-render` correctly → the failure is the command. Fix: `async fn … -> Result<Value, String>` (body unchanged: read-only + clamped).
- **#1 stretched panels:** `.obs-row` default `align-items: stretch` overrode `.outpanel { aspect-ratio: 16/9 }`, collapsing the panels to a ~95 px strip. Fix: `align-items: flex-start` + `.outpanel { max-height: 32vh }` → proper 16:9 monitors (headless-measured 331×186).
- **#2 kind overlap:** `.item .kind` had no truncation → overflowed under the theme dropdown. Fix: `white-space:nowrap; overflow:hidden; text-overflow:ellipsis`.
- **Test integrity:** the headless harness never loaded `app.css` (the injected `<base>` followed the `<link>`, so CSS checks passed via UA fallbacks). Fixed the `<base>` placement + gave the `view` stub a real `OperatorView` so the BOOT render path is exercised (no nav-click crutch) — this class of masking can't recur.

**Independent review** (`wf_6f0621ff-a01`, 3 lenses → refute-by-default verify, ultracode; 4 agents, 268k tokens): **1 raised → 0 confirmed → 1 refuted.** The diagnosis-soundness lens challenged "maybe async fixes nothing, it's all CSS"; the verifier **refuted it + corroborated the diagnosis** — per `app.css` the centered reference text shows ONLY when `.has-render` is absent (render-not-applied = host-side), so the two fixes address two distinct real symptoms; `cargo check` confirmed the async signature is the canonical Tauri v2 pattern. Re-verified: operator fmt/clippy/build/check + `deny bans·licenses·sources` OK; **headless 53/53 with real CSS loading**. See `docs/delivery/CODE-REVIEW-batch-console-render-fix.md`. **Owner on-device QA required** to confirm the render appears on the real Tauri app (this env can't launch the GUI).

**Follow-up registered:** refine **#4** — real OS display names on the Screens page (Tauri monitor enumeration for the standalone/demo backend) → **86ajtxnn1**. **Refine punch-list:** #1 picker ✅ · #2 shapes ✅ · #3 click-select ✅ · #4-menu ✅ · #6 dropdown ✅+refixed(#2 here) · #7 Preview/Live ✅(now actually rendering) — **remaining:** #4 display names (86ajtxnn1), #5 fonts (86ajq3225), #8 scripture live-follow (86ajtwq2b), deferred line shape.

## Stage 8 → Presentation — remote console thumbnails (86ajtwq28, the real #7 fix) (2026-07-31)

**Owner clarified the real setup:** operator + audience output + confidence monitor are **three processes on ONE machine** (multi-monitor, **no network**); the operator drives the `selahcue-desktop` output app over the **loopback** control link (`Backend::Remote`), so the composited pixels live in the *output-app* process — `render_console`'s Remote branch returned `available:false` → the panels showed the reference text. **Fix:** the output app streams its true Preview/Live output as thumbnails over that same loopback link. Handled per the `/build` `backend-engineer` skill + `/goal` (`TASK-86ajtwq28-remote-console-thumbnails.md`, validator PASS 4/4). `selahcue-lan` + `selahcue-app` + `selahcue-operator`; **additive wire (VERSION 2 unchanged)**, RBAC `Monitor` (read), never changes what is on air.
- **Protocol:** `base64` dep; `ThumbView { w, h, rgba }` + `from_rgba` (base64); additive `Command::GetConsoleThumbnails { max_w, max_h }` + `ServerMessage::ConsoleThumbnails { preview, live }`. VERSION 2 unchanged → existing pinned fixtures byte-stable.
- **RBAC + host:** `GetConsoleThumbnails => Monitor`; `LiveController::apply` handles it as a **read** (no state_dirty, no tick), clamps to 480×270, thumbnails `presenter().preview_output()/live_output()` → `ConsoleThumbnails`.
- **Remote + operator:** `RemoteOperator::console_thumbnails` round-trips it; `render_console`'s Remote branch `await`s + forwards the host ThumbViews (transport error → `{available:false}` text fallback). Removed the old `Backend::console_thumbnails`.

**Independent review** (`wf_52c47943-6d7`, 3 lenses → refute-by-default verify, ultracode; 6 agents, 355k tokens): **3 raised → 0 confirmed → 3 refuted.** Each was a real observation below the defect bar: (1) the thumbnail downscale runs under the controller lock (sub-ms, debounced, no on-air/panic/deadlock — a soft-perf note); (2) RBAC `Monitor` sees preview pixels (within its documented "observe live/preview state" scope; consistent with `GetOperatorState`); (3) no per-command timeout on the round-trip (pre-existing across every Remote command; holding the mutex across the await is *mandatory* for reply correlation; loopback can't silently drop; a naive timeout would desync). All recorded as seams. **Verification:** `cargo test --workspace` + `--features server` green (incl. **`remote_operator_fetches_the_hosts_console_thumbnails`** — a full loopback round-trip = the owner's exact path, on-air unchanged); operator fmt/clippy/build + `deny` OK; headless 53/53. See `docs/delivery/CODE-REVIEW-batch-remote-console.md`. **Owner on-device QA** confirms the render appears with the output app running.

**Refine punch-list:** #1 picker ✅ · #2 shapes ✅ · #3 click-select ✅ · #4-menu ✅ · #6 dropdown ✅ · **#7 Preview/Live true render ✅ — now works in the real multi-monitor setup (loopback thumbnails)**. **Remaining:** #4 real display names (`86ajtxnn1`), #5 fonts (`86ajq3225`), #8 scripture live-follow (`86ajtwq2b`), deferred line shape. **Also in flight:** live transcript + scripture detection (subagent, separate branch).

## Stage 8 → Presentation — scripture live-follow (86ajtwq2b, refine #8) (2026-07-31)

**Owner refine #8** — navigating scripture verses now advances **both Preview and Live**, but ONLY when a scripture is already live (owner's decision; preview⟂live isolation preserved otherwise). The preacher's passage follows the audience as the operator scrolls. Handled per `/build` `backend-engineer` + `/goal` (`TASK-86ajtwq2b-scripture-live-follow.md`, validator PASS 4/4). `selahcue-lan` + `selahcue-app` + `selahcue-operator`; **additive wire (VERSION 2)**, RBAC `GoLive`.
- **Design:** a SEPARATE command mapped to `GoLive` RBAC — NOT a `follow` flag on the `SearchScripture`-level `StageScripture` (RBAC is per-command; a flag would let an Assistant escalate to Live). `FollowScripture => GoLive`.
- **Controller:** `apply(FollowScripture)` stages the verse in Preview, then iff `live_scripture.is_some() && go_live()` commits to Live + re-asserts blackout (SetScene reveals). Nothing/non-scripture live → Preview-only.
- **Operator:** `setCursor(i, stage, follow)` — verse NAVIGATION (arrow-scroll, verse click) → `follow_scripture`; a chapter LOAD (search/hit/prev-next/translation) → `stage_scripture` (Preview-only), so searching a new passage never jumps the audience. Double-click "instant live" unchanged.

**Independent review** (`wf_b1b56a81-932`, 3 lenses → refute-by-default; ultracode): **2 raised → 0 confirmed → 2 refuted.** Finding #1 (search/chapter-load also follows) was refuted (the "already live" invariant holds) but **acted on** as the follow/preview verse-nav split above. Finding #2 (Assistant can't preview) refuted (no shipping path pairs Assistant; mobile→Producer, loopback→Operator, both hold GoLive). **Verification:** `cargo test --workspace` + `--features server` green (incl. a full **loopback round-trip** + an **RBAC-denied-over-the-wire** test), operator fmt/clippy/build/deny + headless 53/53. Commit `6370fa6` → **3-OS CI 30636699454 GREEN**. See `CODE-REVIEW-batch-scripture-follow.md`.

**⚠ ClickUp rate-limited (~21h)** — `86ajtwq2b` → qa + BUILD CONTROL `86ajnx548` updates are QUEUED (in the goal contract) to post on recovery. **Refine punch-list:** #1 ✅ #2 ✅ #3 ✅ #4-menu ✅ #6 ✅ #7 ✅ **#8 ✅ (this)** — remaining: #4 real display names (`86ajtxnn1`), #5 fonts (`86ajq3225`); on-device STT (`86ajtxzre`), transcript persistence (`86ajtxzrn`), deferred line shape.

## Stage 8 → Presentation — font weight + letter-spacing (86ajq3225, refine #5) (2026-07-31)

**Owner refine #5** — the Theme Designer's disabled **Weight** + **Letter-spacing** fields are now live: a theme sets a font **weight** (Regular…Black) + **letter-spacing** (tracking) the audience output renders, deterministically (NFR-014), additively (no wire/migration break). Handled per `/build` `backend-engineer`+`frontend-engineer` + `/goal` (`TASK-86ajq3225-font-weight-letter-spacing.md`, validator PASS). `selahcue-engine` + `selahcue-present` + `selahcue-operator`; **no wire/RBAC/migration change** (`VERSION` unchanged); GPU-skip unaffected (Text already skipped).
- **Engine:** `TextStyle { weight, letter_spacing_px }` on `Layer::Text` as `Option<TextStyle>` (additive, Eq, skip-if-default → a Regular/0 layer is **byte-identical** to before). `draw_text` applies `Attrs::weight` + a deterministic **faux-bold** integer smear for the single-weight bundled font (cosmic-text 0.12 does **not** synthesize bold here — verified empirically) + per-glyph tracking; a **system** font uses its real weight face.
- **Present:** `Theme` gains `weight: u16` (skip-if-400) + `letter_spacing_permille: i16` (skip-if-0); `compose` threads them (per-mille → px via the region cell). Default theme JSON byte-identical.
- **Designer:** `#td-weight` (Regular/Semibold/Bold/Black) + `#td-letter` (em) enabled + wired to the previewed/saved theme (deleted at 400/0 → byte-stable).

**Independent review** (`wf_66ba08ec-5b7`, 3 lenses → refute-by-default; ultracode; 9 agents, 494k tokens): **6 raised → 4 confirmed (2 distinct defects) → 2 refuted. Both defects fixed + regression-tested.** (1) **MEDIUM panic regression** — `raster.rs:741` `2 * px as i32` overflowed i32 for `px ≥ 2³⁰` (DEBUG multiply-overflow / RELEASE min>max), firing even at the default style; fixed via an i64+saturate bound, pinned by `extreme_px_text_does_not_overflow_panic`. (2) **LOW wrong-render** — `measure_line_width` omitted letter-spacing so auto-fit fit the untracked width while `draw_text` added `(glyphs−1)·ls` → tracked text clipped right (FR-010 violation); fixed by folding positive tracking into `wrap_at` (per-glyph + per-space, conservative, O(words)), pinned by `tracked_text_fits_its_region_no_right_clip`. Refutations correct (intentional ls≥0 cull guard; cosmetic app-unproducible weight-select gap). **Verification:** `cargo test --workspace` **466 passed / 0 failed**, `--features server`/`encryption` green, fmt/clippy clean, operator `node --check` + headless **58/58**. Commit `362c5a9` → **3-OS CI `30642394688` GREEN** (all 11 jobs). See `CODE-REVIEW-batch-font-weight.md`.

**⚠ ClickUp rate-limited (~21h)** — `86ajq3225` → qa + BUILD CONTROL `86ajnx548` updates are QUEUED (in the goal contract) to post on recovery. **Deferred with seams:** per-**region** weight/spacing; multi-weight/family bundling + custom-font import (FR-173); variable-font axes; kerning; gradient/image backgrounds; full `Fit::Paginate`. **Refine punch-list:** #1 ✅ #2 ✅ #3 ✅ #4-menu ✅ #6 ✅ #7 ✅ #8 ✅ **#5 ✅ (this)** — remaining: #4 real display names (`86ajtxnn1`); on-device STT (`86ajtxzre`), transcript persistence (`86ajtxzrn`), deferred line shape.

## Stage 8 → Quality — code audit: memory · view-load · scripture→output latency (2026-07-31)

**Owner-requested audit** (read-only + measurement, NOT a feature batch): memory leaks, slow-loading views, and the scripture→output response time. Handled per `/build` `qa-engineer` + `/goal` (`TASK-audit-memory-perf-latency.md`, validator PASS 4/4 `--require-complete`). Deliverable: **`docs/quality/AUDIT-memory-perf-latency.md`**. Only the one timing test + the report were added — no product code changed; the uncommitted font batch was audited as-is.
- **Method:** a `Workflow` fan-out (`wf_f7726032-3d7`, ultracode; 3 lenses — memory-rust, memory-webview, viewload-webview) reading the code; a deterministic timing test; the two headline findings hand-verified.
- **Verdict:** **NO HIGH finding, no unbounded anonymously-reachable OOM path.** The no-leak rule is largely satisfied — 7 structures bounded **and** flood-tested (glyph/image caches, transcript/detection rings, saved-theme library, assignments, pairing offers). Webview listener/timer hygiene clean (22 addEventListener / 0 leak; init-once on permanent nodes). **5 MEDIUM + 4 LOW gaps** queued as follow-ups.
- **Memory:** M2 `ServicePlan.items` no `MAX_PLAN_ITEMS` cap/test (persisted+reloaded unbounded, `EditPlan`-gated); M3 LAN `active` session map no hard cap/idle-TTL (operator-gated); M4 transcript DOM bounded only by the host tail (no client cap/test); L1 `recent_refs` capped-but-untested.
- **View-load:** **M1 (headline, Verified)** — the Live Console **plan list rebuilds every second** during a countdown because its dedup key is `JSON.stringify(view)` incl. `view.timer` (the console sig excludes it, the plan key doesn't) → ~1,700 nodes/sec torn down for a 40-item plan; 1-line fix = narrow the key. M5 console RGBA per-byte base64 decode on the main thread (well-gated). L3 Theme Designer eager font-list load at startup. Scriptures (chapter-at-a-time, no N+1) + Screens (change-keyed) credited as good.
- **Latency (C-003):** **median 118.7ms** @1920×1080 for `StageScripture+GoLive` (p90 125.3 / max 126.4). Budget: one compose+render ≈59ms (compose ≈8.6ms, **raster ≈50.5ms ≈85%**); the 118ms ≈ two compose+renders (preview then live) + sub-ms parse/lookup. Local multi-monitor = no LAN hop; Tauri `invoke` loopback is sub-ms. Acceptable for a one-shot transition; raster is the lever if snappier is ever wanted.

**⚠ ClickUp rate-limited (~21h)** — the 6 follow-up tickets + a BUILD CONTROL `86ajnx548` update are QUEUED in §4 of the report to post on recovery.

## Stage 8 → Quality — audit M1 fix: plan-render dedup narrowing (86ajnx548) (2026-07-31)

**Audit follow-up M1 (owner chose "fix now").** The Live Console plan list was torn down + rebuilt **every second** during a countdown because its dedup key was `JSON.stringify(view)` (incl. `view.timer`). Narrowed the key to exactly the fields the plan render reads — `JSON.stringify([view.plan_name, view.items, view.themes, view.saved_themes])` — mirroring the console-render sig that already excluded `view.timer`. Now the plan rebuilds only on genuine plan changes. Handled per `/build` `frontend-engineer` + `/goal` (`TASK-86ajnx548-plan-render-dedup.md`, validator PASS 2/2 `--require-complete`). **Operator-webview only** (`dist/app.js`); no host/wire/RBAC/persistence change.
- **Field-set proof:** the plan-build block reads only `plan_name`/`items` (per-item `is_live`/`is_staged`/`title`/`kind`/`slide_*`/`id`/`theme` nested inside `items`)/`themes`/`saved_themes`; timer/transcript/detections/blackout/global-theme/screen_themes are read only by the sibling syncs (each with its own change-key, running before the plan early-return).
- **Independent review** (`wf_856f033b-706`, 2 lenses — completeness + regression → refute-by-default; ultracode): **6 findings, ALL INFO, 0 defects.** Regression lens cross-checked `operator.rs` ItemView + `controller.rs` (per-item flags nested in each item → captured by `JSON.stringify(view.items)`); confirmed the `editing`/`confirmDelete`/focused-defer paths are unaffected (they fire before the key compare; cancelled-edit restore is `act()`-driven) and no plan-visible change is carried only by an excluded field.
- **Verification:** `node --check` clean; operator headless **61/61** (+3 M1: timer-only delta keeps node identity = no rebuild; items delta = 2 fresh rows). Commit `<pending>` → 3-OS CI `<pending>`. See `CODE-REVIEW-batch-plan-dedup.md`.

**⚠ ClickUp rate-limited** — closes audit follow-up **#4**; `86ajnx548` update queued. **Remaining audit follow-ups:** M2 `ServicePlan.items` cap, M3 LAN session cap, M4 transcript DOM client cap, M5 console RGBA decode, + LOW cluster.
