1. On the Operator Console, use local keys for commands 
2. Output Window. Select which output to display (Main, Stage,Livestream, etc)
3. Choose theme for each output, or for each item 
4. Show the TimeUp output automatically once time runs out